<!-- i18n-nav -->
[中文](P%E6%B3%A2%E6%96%87%E6%A1%A3) | [English](P%E6%B3%A2%E6%96%87%E6%A1%A3.en.md) | [日本語](P%E6%B3%A2%E6%96%87%E6%A1%A3.ja.md)
<!-- /i18n-nav -->

# 多导联 ECG P 波 onset/offset 精确定界

**技术方案：可靠导联筛选 + 稳健融合（纯信号处理路线，不使用机器学习）**

---

## 0. 方法学边界

本方案不使用任何需要标注数据训练的模型。以下方法**不属于机器学习**，均可使用——它们没有训练集、没有学习到的参数，全部是逐拍在线计算的确定性变换：

| 可用（确定性 DSP / 数值方法） | 排除（需训练） |
|---|---|
| 小波变换、模极大值分析 | U-Net / DENS-ECG 等分割网络 |
| PCA / SVD 空间投影（每拍窗内现算） | 训练得到的分类器、回归器 |
| Kalman 滤波、中位数滤波 | 任何依赖标注数据集拟合的参数 |
| 非线性最小二乘曲线拟合（LM） | |
| 模板相关（模板由本条记录自身叠加得到） | |
| Kors / Dower 变换矩阵（公开固定常数） | |

**放弃网络后的主要损失**不是定界精度，而是逐导联的软证据（每样本概率）。第 4 节专门解决这个问题。

---

## 1. 问题特性与精度上限

P 波振幅通常 **50–250 µV**，主要能量在 **15 Hz 以下**，与基线漂移、呼吸调制、肌电频带均有重叠。onset/offset 本质是**低斜率转折点**的判定，即「信号斜率降到噪声水平以下」的时刻。

因此最终精度上限由 **SNR 与基线稳定度**决定，算法只能逼近这个上限而不能超越。

**评估标尺（CSE 专家间一致性，2SD）**

| 边界 | 容差 |
|---|---|
| P onset | 10.2 ms |
| P offset | 12.7 ms |
| QRS onset | 6.5 ms |
| QRS offset | 11.6 ms |
| T offset | 30.6 ms |

---

## 2. 预处理（影响大于算法选择）

### 2.1 基线去除
- **首选**：只用已经确认位于上一拍 T offset 与本拍 P onset 之间的 TP
  等电位点作为三次样条节点。**PQ/PR 段包含 Ta（心房复极）成分，不得默认
  当作等电位线。**
- 高心率使 TP 段消失时，不再声称存在“实测基线”：只允许用 P 候选两侧的
  低阶连续趋势做局部重构，并将其标记为低置信、禁止单独触发边界覆盖。
- 次选：零相位高通（filtfilt），截止 ≤ 0.5 Hz。
- ⚠️ **避免单向 IIR 高通**。0.5 Hz 以上的高通会系统性地把 P onset 后推、把 P 幅度压低，且各导联偏移一致，稳健融合无法发现这种**共模误差**。

### 2.2 低通 30–40 Hz
P 波几乎无 30 Hz 以上成分，砍掉可显著提升 SNR，零相位前提下对 onset 位置几乎无偏移。

### 2.3 上采样至 1000 Hz
原始 250 Hz 时单纯量化误差即达 4 ms。插值后再定界可回收数毫秒。

### 2.4 选择性叠加平均
若任务允许（非逐拍分析），按形态聚类后对同类 P 波做 R 波或 P 峰对齐平均，SNR 提升 √N。这是 SAECG 的做法，对低幅 P 波是性价比最高的一招。

> 异位 P 与窦性 P 必须先分簇，**绝不跨形态平均**。

---

## 3. 单导联确定性定界方法

| 方法 | 适用点 | 注意事项 |
|---|---|---|
| **小波模极大值**（Martínez 2004） | 主力方法，尺度 2⁴/2⁵，抗噪好 | 双相 P（尤其 V1）必须显式区分 `+−` / `−+` / `+−+` 三种模板，不能死用「第一个极大值」规则 |
| **相量变换**（Phasor Transform） | 专为低幅 P 波设计，把小幅度变化映射为大相角变化 | P 峰定位极佳，边界仍需配合阈值判定 |
| **切线法** | 简单、可解释 | 单点导数太抖；改为在最大斜率段做**最小二乘拟合直线**再与基线求交，稳定性提升约一个量级 |
| **面积 / 梯形法** | P offset（低斜率终点）优于阈值法 | 依赖可靠的基线参考 |
| **曲线长度变换 / 滑窗绝对导数积分** | 生成平滑「活动度」包络，适合做全局检测函数 | 窗长决定时间分辨率，P 波取 20–30 ms |
| **高斯 / 偏态高斯 LM 拟合** | 亚采样点精度，残差天然可作置信度 | 初值需良好，建议用小波结果做初值 |

### 3.1 推荐组合

```
小波模极大值  →  定位 P 峰 + 判定极性模式
      ↓
窄窗内精定界：最小二乘切线法（onset）+ 面积法（offset）
      ↓
高斯拟合交叉校验  →  三者分歧大即判为低置信
```

三种方法的失效模式互不相关，这正是分歧度可作为置信度的原因。

### 3.2 等电位窗检验（自适应关键步骤）

从 P 峰向前搜索，找**最后一个**长度 W（15–20 ms）的窗口，使窗内斜率绝对值与方差同时低于阈值。

**阈值不是常数**，而是从该拍经过 T offset 验证的 TP 段实测噪声水平按比例
导出（如 3σ_noise）。PR/PQ 段包含 Ta，不能作为噪声窗；如果没有足够长度的
TP 段，局部 SNR 必须报不可用，边界只能走低置信 fallback。

---

## 4. 无 ML 的置信度构造（替代 softmax 的核心）

每导联每拍需输出一个边界不确定度 σᵢ，供后续加权融合使用。以下方法按性价比排序：

1. **阈值扰动法（首选，近乎零成本）**
   把小波阈值 ξ 在 3%–10% 范围内扫 5–7 个值，得到一组 onset 估计。
   - 点估计 = 这组值的中位数
   - **不确定度 σᵢ = 这组值的离散度**

2. **多尺度一致性**
   在 2³、2⁴、2⁵ 三个尺度分别定界。尺度间偏差大 → 该导联 P 波被噪声主导。

3. **多方法一致性**
   切线法 vs 面积法 vs 拟合法三者的分歧度。

4. **噪声注入蒙特卡洛**
   按经过 T offset 验证的 TP 段实测噪声水平生成同色噪声叠加 10 次，重新定界
   取方差。没有 TP 段时不得借用 PR 段噪声冒充。

5. **拟合残差**
   高斯拟合的归一化残差。

6. **拍间抖动**
   同导联最近 N 拍 onset 标记的 MAD（无需真值即可评估可靠性）。

有了 σᵢ，后续加权融合与「有网络」时完全一致：权重 **wᵢ ∝ 1/σᵢ²**，再乘 SNR 门控。

---

## 5. 多导联全局检测函数

这是商用 ECG 程序（Marquette / Glasgow 一类）数十年的标准做法，完全不依赖 ML。

### 5.1 空间速度函数（Spatial Velocity）—— 推荐主力

$$SV(t)=\sqrt{\sum_{i\in\mathcal{L}} w_i\left(\frac{dx_i}{dt}\right)^2}$$

对 8 个独立导联（或 VCG 的 X/Y/Z）求导数向量的模。

**原理**：心房激动开始的瞬间，**所有导联同时**出现电位变化，故 SV 在 P onset 处有明确抬升；而各导联独立噪声在平方求和后不相干叠加，SNR 按 √L 改善。

**判定**：P onset = SV 首次并持续超过 k·σ_SV(已验证 TP 段) 的时刻。
若 TP 不存在，则该自适应阈值证据不可用。

**实践要点**
- 导数用 **Savitzky-Golay**（2 阶、25–35 ms 窗），不用简单差分，否则噪声被放大。
- **只纳入通过质量门限的导联**，坏导联进入平方和只会抬高本底。
- SV 给出的是「全局最早」，与 CSE 金标准定义**口径一致**——这点比 SVD 投影更对口。

### 5.2 SVD 空间投影 —— 保留作互补

每拍在 P 窗内对多导联数据矩阵做 SVD，投影到第一奇异向量（P 环主方向）。纯线性代数运算。

**关键细节**
- 投影方向必须**只在 P 窗内估计**（含 T 或 QRS 会污染主方向）。
- 必须**迭代**：粗 P 窗 → 投影 → 定界 → 收窄窗 → 重新投影，通常 2 轮收敛。
- 优点：自动规避「P 轴垂直于某导联导致该导联 P 近乎等电位」这一致命问题。
- 注意口径：投影得到的 onset 更接近「真实三维激动起点」，与 CSE「各导联中最早者」的定义存在系统性偏差。

### 5.3 两者互补

- **SV** 强调「有无变化」
- **投影** 强调「最大 P 能量方向」

两者结果不一致本身就是一个有价值的警报信号。

其他等价全局函数：各导联绝对导数加权和、RMS 合成导联、VCG 向量幅度。通常 SV 最锐利。

---

## 6. 可靠导联判据（全部纯 DSP）

| 判据 | 说明 |
|---|---|
| **P 波局部 SNR** | P 峰幅度 / 已验证 TP 段噪声 RMS —— **权重主项**；TP 消失时为 N/A |
| 模板相关系数 | 当前拍 P 波与同导联在线叠加模板的相关系数，< 0.8 降权 |
| 基线平稳度 | PQ / TP 段等电位水平差、残余斜率 |
| 形态门限 | P 时限 60–140 ms、幅度 0.03–0.35 mV、P–QRS 间期合理；越界直接剔除 |
| 拍间稳定性 | 最近 N 拍 onset 标记的 MAD |
| 一致性指标 | 第 4 节的阈值扰动 / 多尺度 / 多方法离散度 |
| 导联脱落 / 饱和 / 平线 | 硬性排除 |

### ⚠️ 最重要的一条：等电位导联判定

若某导联 P 幅度 < 2σ_noise，**标记为「无信息」，权重置 0，绝不输出边界**。

多导联融合的多数翻车案例，根源都是把等电位导联上的噪声当成 P 波在用。

---

## 7. 稳健融合

### 7.1 不要直接取 min / max
CSE 的「最早 onset、最晚 offset」定义对单个坏导联极度敏感。

**稳健替代**：在 SQI 达标的导联集合内取次序统计量——
- 第 **2** 早的 onset（而非最早）
- 第 **2** 晚的 offset（而非最晚）
- 或取 15 / 85 百分位数

### 7.2 加权稳健估计
加权中位数或 **Huber M 估计**，权重 wᵢ ∝ SQIᵢ / σᵢ²。

### 7.3 迭代重加权剔除
```
计算稳健中心
  → 偏离 > 2.5 × MAD 的导联权重置 0
  → 重算
  → 通常 2 轮收敛
```

### 7.4 跨导联一致性硬约束
同一次心房激动，各导联 onset 的真实离散度应 **< 20–30 ms**。

剔除离群后若离散度仍超标 → **输出「不可信」，不要硬给数字**。这通常意味着存在系统性问题：房扑、大面积伪差、导联接错。

---

## 8. 拍间时间一致性

- 对 **PR 间期**与 **P 时限**序列做中位数滤波或标量 Kalman 平滑。
- 突变超过阈值的拍标为异常。
- **先按模板相关性分簇**（窦性 P / 异位 P），**绝不跨簇平滑**。

---

## 9. 推荐落地流水线

```
① 预处理
   样条去基线（仅用已验证 TP 等电位点）
   TP 消失 → 连续低阶局部趋势、低置信标记
   + 40 Hz 零相位低通
   + 上采样至 1000 Hz
        ↓
② 参考点检测
   QRS 检测 → T offset 检测
   → 依 RR 自适应给出 P 搜索窗
        ↓
③ 多导联粗定界
   SV 函数 → 全局 P 区间
   + 「P 是否存在」门控（房颤 / 房扑在此拦截）
        ↓
④ 单导联精定界（窄窗下发）
   小波模极大值 + 最小二乘切线法 / 面积法
        ↓
⑤ 置信度评估
   阈值扰动法 → σᵢ
   质量指标   → SQIᵢ
        ↓
⑥ 稳健融合
   加权中位数 + 迭代重加权
   → 全局 P onset / offset
        ↓
⑦ 后处理
   拍间中位数 / Kalman 平滑
   + 跨导联一致性校验
   → 输出边界及置信度（或「不可信」标记）
```

窄窗下发（步骤 ③→④）是核心设计：既拿到全局函数的 SNR 红利，又保留单导联的时间分辨率，同时大幅降低误检。

---

## 10. 参数标定（无训练集情况下）

用**已知真值的仿真信号**标定：

1. 用 ECGSYN 生成，或对真实低噪 P 波做形态模板。
2. 按可控 SNR 叠加**真实噪声**——MIT-BIH Noise Stress Test Database 的 `bw`（基线漂移）/ `ma`（肌电）/ `em`（电极运动）记录。
3. 扫参数，观察误差的 mean 与 SD。
4. 选 **SD 最小**的参数，并把 **mean 作为固定偏差扣掉**。
5. 在 LUDB / CSE 上做独立验证。

> 这是**标定**而非训练——参数是少数几个物理意义明确的常数（阈值比例、窗长、导数阶数），不存在过拟合风险。

---

## 11. 常见坑

| 问题 | 对策 |
|---|---|
| **Ta 波污染 PR/PQ 基线** | PR/PQ 不作为 P 噪声窗或等电位节点；Ta 是低频生理成分，不得被 detrend 后误标成“低噪声” |
| **高心率、TP 消失或 P-on-T** | 先由上一拍 T offset 判断是否仍有可观测 TP。无 TP 时只用连续低阶趋势生成低置信检测视图，保留原始边界且禁止激进的 earliest/latest 融合。T 模板相减默认关闭 |
| **确需 T/QRST 对消** | 只能在同形态簇、逐拍 ST-T 时移与幅值适配、低通检测视图、接缝 taper、残差高频伪影门控全部通过时作为 second opinion；不能用固定 R 对齐模板逐点硬减。全程高心率且 P 与 T 锁相重叠时两者不可辨识，应拒判而不是硬分离 |
| **房颤 / 房扑** | 必须有前置的「P 波存在性判别」门控。否则定界器会在 f 波上稳定输出错误边界，且置信度还很高 |
| **滤波器相位偏移** | 任何非零相位滤波都会让 onset 系统性偏移，且各导联偏移一致 → 稳健融合发现不了。上线前用仿真信号标定整条滤波链的固有偏差 |
| **双相 P（V1）** | 模极大值模式与单相不同，必须显式处理三种模板 |
| **等电位导联** | 见第 6 节，权重置 0 |
| **系统偏差 vs 离散度** | 偏差可标定扣除，**SD 才是真本事**，评估时必须分开报告 |

---

## 12. 评估基准

| 数据库 | 特点 | 用途 |
|---|---|---|
| **LUDB** | 200 条，12 导联，10 s，全波形标注 | 多导联方案的首选 |
| **QT Database** | 105 条，2 导联，拍数多 | 单导联方法验证 |
| **CSE Multilead Measurement DB** | 多导联定界官方标尺 | 最终符合性验证 |
| **MIT-BIH Noise Stress Test DB** | 真实噪声记录 | 参数标定、鲁棒性测试 |

**报告方式**：给出 mean ± SD，并将 SD 与 CSE 容差（P onset 10.2 ms / P offset 12.7 ms）对比。

---

## 13. 预期性能与边界

在**信噪比尚可**的 12 导联静息心电上，纯 DSP 方案的定界精度与深度模型基本持平。

差距主要出现在：
- 强肌电干扰
- 病理形态（房内传导阻滞的碎裂 P、双相深倒置 P）

这部分靠**加严门控、宁可拒判也不硬判**来兜底。给出一个明确的「不可信」标记，通常比给出一个错误但高置信的边界更有价值。
