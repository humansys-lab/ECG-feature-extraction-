<!-- i18n-nav -->
[中文](%E6%B5%81%E7%A8%8B) | [English](%E6%B5%81%E7%A8%8B.en.md) | [日本語](%E6%B5%81%E7%A8%8B.ja.md)
<!-- /i18n-nav -->

# 12 导联心电图自动判读系统 —— 设计与实施规格书

**版本** v1.0 ｜ **技术路线** 确定性信号处理 + 显式规则推理（不含机器学习）

---

## 0. 文档说明

### 0.1 目的

本文档定义一套 12 导联静息心电图自动判读系统的完整工程规格，覆盖从原始采样数据到结构化判读报告的全部处理环节。文档面向算法工程师与系统架构师，每一节均给出可直接进入编码的参数、判定式与验收条件。

### 0.2 技术路线约束

**本系统不使用任何机器学习方法。** 具体含义：

- 波形检测与定界使用小波变换、自适应阈值、形态学算子等确定性算法
- 心搏分型使用模板互相关与时限规则，不使用分类器
- 诊断推理使用显式规则库与专家标定的评分表，权重由人工设定并可追溯到文献
- 所有阈值集中在配置表中管理，不存在从数据中拟合得到的参数

**这一约束带来的直接后果**（必须在项目立项时被接受）：

| 影响面 | 后果 |
|---|---|
| 可解释性 | 完全可解释，任一结论可回溯到具体导联、具体测量值、具体判据条款 |
| 监管递交 | 显著简化。无需提交训练集代表性、数据漂移监控、再训练变更控制等材料 |
| 开发成本 | 前期低（无需标注数据），后期高（规则库维护与边界修补是长期工作） |
| 敏感度 | 对典型表现的病例敏感度良好；对非典型形态学（如 de Winter、Aslanger 型）敏感度明显低于形态学模式识别方法 |
| 噪声鲁棒性 | 弱于学习型方法。因此第 5 章的质量门控是本系统的**关键安全屏障**，不是可选模块 |

设计上的应对策略是：**提高弃权率而不是提高误判率**。系统宁可输出"信号质量不足 / 形态学复杂，建议人工判读"，也不给出低置信结论。

### 0.3 预期用途声明

本系统为**辅助判读工具**，输出为供医师复核的参考信息，不构成独立诊断。所有报告须标注该声明。任何情况下不得设计为无人复核的闭环诊断路径。

### 0.4 术语与符号

| 符号 | 含义 |
|---|---|
| `fs` | 采样率（Hz） |
| `L` | 导联集合，`L = {I, II, III, aVR, aVL, aVF, V1..V6}` |
| `N_beat` | 一份记录内检出的心搏总数 |
| `RR[i]` | 第 i 个 R 峰到第 i+1 个 R 峰的间期（ms） |
| 中位搏 | median beat，由主导型心搏逐点取中位数合成的代表性波形 |
| finding | 发现，由连续测量值离散化得到的布尔或分级中间语义项 |
| statement | 陈述，最终输出的诊断性结论条目 |
| 门控 | gating，某模块的执行与否取决于上游模块的判定结果 |

---

## 1. 总体架构

### 1.1 分层数据流

```
                    ┌──────────────────────────────────────┐
   原始采样数据  ─▶ │ L0  数据接入与标准化                  │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L1  预处理（滤波）                    │──▶ 保留未滤波支路
                    └──────────────┬───────────────────────┘    （起搏钉检测用）
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L2  信号质量评估 + 导联合法性校验     │
                    └──────────────┬───────────────────────┘
                                   │ 质量门控（可能在此终止）
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L3  波形检测与定界                    │
                    │     R 峰 → QRS 起终点 → T 终点 → P    │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L4  心搏模板聚类 + 中位搏合成         │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L5  特征量化                          │
                    │     全局 / 逐导联 / 逐拍 / 派生指数   │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L6  发现层（连续量 → 离散 finding）   │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L7  诊断规则推理（按门控 DAG 顺序）   │
                    │     M0 起搏 → M2 传导 → M1 节律 →     │
                    │     M3 肥大 → M4 缺血 → M5 复极 → M6  │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L8  冲突消解 / 优先级排序 / 序列比对  │
                    └──────────────┬───────────────────────┘
                                   ▼
                    ┌──────────────────────────────────────┐
                    │ L9  报告生成（陈述 + 证据链 + 弃权）  │
                    └──────────────────────────────────────┘
```

### 1.2 设计不变式

以下四条是架构的硬约束，任何优化都不得违反：

1. **测量与推理严格分离。** L5 及以下只产生数值，不产生任何医学判断；L7 只消费 finding，不直接访问原始信号。
2. **单向数据流。** 下游不得回写上游结果。唯一例外是 L3 内部的漏搏回溯搜索（见 6.3）。
3. **每个 finding 与 statement 必须携带证据引用。** 引用格式为 `(导联, 测量项, 数值, 阈值, 判据条款号)`。无法生成完整证据引用的结论不得输出。
4. **弃权优先于猜测。** 任一层判定输入不可靠时，向下游传递 `UNRELIABLE` 标志而非默认值；下游遇此标志的必要输入时必须弃权。

### 1.3 模块接口契约

| 模块 | 输入 | 输出 | 失败模式 |
|---|---|---|---|
| L0 | 设备原始文件 | `Recording{signal[12][n], fs, gain, age, sex, filter_meta}` | 缺字段 → 终止，报"数据不完整" |
| L1 | `Recording` | `FilteredSignal`, `RawSignal` | 无（滤波不失败） |
| L2 | `FilteredSignal` | `QualityReport{sqi[12], usable_leads, lead_swap_flag}` | 全导联不可用 → 终止 |
| L3 | `FilteredSignal`, `QualityReport` | `Delineation{beats[], fiducials}` | 检出心搏 < 3 → 终止 |
| L4 | `Delineation` | `BeatClasses[]`, `MedianBeat[12]` | 主导类占比 < 30% → 标记 `MORPH_COMPLEX` |
| L5 | `MedianBeat`, `Delineation` | `FeatureSet`（见第 7 章） | 单项不可测 → 该项置 `NULL` |
| L6 | `FeatureSet` | `Findings[]` | 依赖特征为 `NULL` → finding 置 `UNKNOWN` |
| L7 | `Findings[]` | `Statements[]` | 门控前置未满足 → 该模块跳过 |
| L8 | `Statements[]` | `RankedStatements[]` | 无 |
| L9 | `RankedStatements[]` | `Report` | 无 |

### 1.4 三值逻辑约定

所有 finding 取值域为 `{TRUE, FALSE, UNKNOWN}`。规则引擎按三值逻辑求值：

- `TRUE AND UNKNOWN = UNKNOWN`
- `FALSE AND UNKNOWN = FALSE`
- `TRUE OR UNKNOWN = TRUE`
- `FALSE OR UNKNOWN = UNKNOWN`

诊断规则求值为 `UNKNOWN` 时，输出弃权条目而非否定结论。**这一条是整个系统安全性的核心**，实现时必须有单元测试覆盖。

---

## 2. L0 数据接入与标准化

### 2.1 输入要求

| 项 | 要求 | 说明 |
|---|---|---|
| 采样率 | ≥ 500 Hz，推荐 1000 Hz | 低于 500 Hz 时 QRS 时限量化误差 ≥ 2 ms，起搏钉可能完全丢失 |
| 分辨率 | ≤ 5 μV/LSB | |
| 时长 | ≥ 10 s | 短于 8 s 时禁止输出节律类结论 |
| 导联 | 8 个独立导联（I, II, V1–V6） | III、aVR、aVL、aVF 由 I、II 计算导出，见 2.3 |
| 元数据 | 年龄、性别、设备滤波设置 | 年龄性别缺失时，第 10、11 章多条判据无法求值 |

### 2.2 强制元数据

**年龄与性别不是可选字段。** 以下判据依赖性别分层：Cornell 电压、Cornell 乘积、Peguero-Lo Presti、QTc 阈值、V2–V3 ST 抬高阈值。以下判据依赖年龄分层：V2–V3 ST 抬高阈值（40 岁分界）、心率正常范围。缺失时对应 finding 置 `UNKNOWN`。

对缺失情况的处理策略选择（**需在项目层面决策并写入配置**）：

- 策略 A（保守，推荐）：置 `UNKNOWN`，相关诊断弃权
- 策略 B（激进）：取最严格阈值（如按女性阈值判 Cornell），并在报告中标注"性别未知，按保守阈值判定"

### 2.3 导出导联计算

若设备只提供 8 导，按下式导出：

```
III  = II - I
aVR  = -(I + II) / 2
aVL  = I - II/2
aVF  = II - I/2
```

**注意**：若设备已提供 12 导全部数据，**不要**重新计算，直接使用设备值，并做一致性校验（导出值与设备值差异 > 50 μV 的样本点比例 > 5% 时标记数据异常）。

### 2.4 单位标准化

内部统一使用 μV 与 ms。转换在 L0 完成一次，此后所有模块不再做单位换算。**这是防止量纲 bug 的关键约定，代码评审时应作为检查项。**

### 2.5 设备滤波元数据的用途

不同设备的前置滤波设置会系统性改变 ST 段水平与 QRS 振幅。L0 须记录设备的高通/低通截止频率，并在以下场景使用：

- 设备高通 > 0.05 Hz：ST 段测量结果标注"可能受滤波影响"，ST 相关判据的置信度降一级
- 设备低通 < 100 Hz：QRS 振幅类判据（Sokolow-Lyon、Cornell）置信度降一级
- 设备已开启"肌电滤波"或"平滑"：切迹/碎裂 QRS 相关 finding 置 `UNKNOWN`

---

## 3. L1 预处理

### 3.1 滤波链

处理顺序：**基线校正 → 工频抑制 → 低通**。顺序不可交换。

#### 3.1.1 基线漂移校正

推荐方案：**级联中值滤波**（相位失真为零，对 ST 段最安全）

```
step1 = medfilt(x, win = 200 ms)    # 消除 QRS
step2 = medfilt(step1, win = 600 ms) # 消除 P、T
baseline = step2
y = x - baseline
```

窗长须为奇数样本点。`fs = 500` 时 win1 = 101 点，win2 = 301 点。

备选方案：零相位高通 IIR，截止 0.05 Hz，采用前向-后向滤波（`filtfilt`）实现零相位。

**禁止使用单向 IIR 高通。** 单向 IIR 在 QRS 后引入的相位失真会造成人为的 ST 段压低，这是自研系统最常见的系统性错误之一。

若采用线性相位 FIR，截止频率可放宽至 0.67 Hz（等效诊断性能）。

#### 3.1.2 工频抑制

优先级顺序：

1. **自适应陷波**：以电源频率为中心的 LMS 自适应对消，Q 值 ≤ 30
2. 若无法确定电源频率（50/60 Hz），先在 45–65 Hz 做谱峰检测自动判定
3. 备选：滑动平均消除法（窗长 = 1 个工频周期的整数倍）

**禁止使用高 Q 值静态陷波**（Q > 50）。高 Q 陷波在 QRS 后产生振铃，会被下游误判为切迹或 J 波。

#### 3.1.3 低通

| 对象 | 截止频率 | 阶数 |
|---|---|---|
| 成人 | 150 Hz | 零相位 FIR，阶数按 fs 计算使过渡带 < 20 Hz |
| 12 岁以下儿童 | 250 Hz | 同上 |

`fs = 500 Hz` 时，250 Hz 低通无意义（等于 Nyquist），儿科场景须 `fs ≥ 1000 Hz`。

### 3.2 未滤波支路

保留一路仅做基线校正、**不做低通**的信号，专供起搏脉冲检测使用（见 8.5）。起搏钉宽度可低至 0.1 ms，任何低通都会将其抹平。

### 3.3 测量专用支路

ST 段与振幅测量应在**中位搏**上进行（见第 6 章），中位搏本身即是强力的噪声抑制手段。不要为了"更干净"而对测量支路追加额外滤波。

---

## 4. L2 信号质量评估与导联合法性校验

本章是系统的安全屏障。规则系统对噪声的鲁棒性弱于学习型方法，**质量门控做不好，后面所有精细的判据都是自欺欺人**。

### 4.1 逐导联质量指标

对每个导联独立计算下列指标，全部为 [0,1] 归一化或布尔值：

| 指标 | 定义 | 不合格阈值（默认） |
|---|---|---|
| `flatline` | 滑动 1 s 窗内峰峰值 < 50 μV 的时间占比 | > 10% |
| `saturation` | \|x\| ≥ 0.95 × ADC 满量程的样本占比 | > 0.5% |
| `baseline_drift` | 0.05–0.5 Hz 频带能量 / 0.05–40 Hz 总能量 | > 0.30 |
| `powerline` | (f0 ± 1 Hz) 频带能量 / 0.05–100 Hz 总能量 | > 0.15 |
| `emg` | TQ 段内 40–100 Hz 能量 / TQ 段总能量 | > 0.40 |
| `pSQI` | 5–15 Hz 能量 / 5–40 Hz 能量 | < 0.40 或 > 0.90 |
| `kSQI` | 信号峰度 | < 5（正常 ECG 峰度高，低峰度提示噪声主导） |
| `bSQI` | 两个独立 QRS 检测器结果的一致率 | < 0.85 |

`bSQI` 需要两个原理不同的 QRS 检测器（如 Pan-Tompkins 与小波模极大值法）。二者检出位置差 < 100 ms 视为一致。**这是最有效的单一质量指标，优先实现。**

### 4.2 导联可用性判定

```
lead_usable[l] = NOT flatline AND NOT saturation
                 AND baseline_drift < T_bd
                 AND emg < T_emg
                 AND bSQI > T_b
```

### 4.3 整体门控决策

| 条件 | 动作 |
|---|---|
| 可用导联 ≥ 10 且含 II、V1、V5 | 正常执行全部模块 |
| 可用导联 6–9 | 执行，但涉及不可用导联的判据置 `UNKNOWN` |
| 可用导联 < 6，或 II 与 V1 均不可用 | **终止**，输出"信号质量不足，建议重新采集" |
| `bSQI` 全导联均 < 0.7 | **终止**，同上 |

### 4.4 导联反接检测

反接检测必须在任何诊断之前完成。误将反接判为病理是临床上最危险的错误类型之一。

#### 4.4.1 肢导反接的确定性规则

| 反接类型 | 特征表现 | 判定式 |
|---|---|---|
| LA ↔ RA | I 导 P/QRS/T 全部倒置；II 与 III 互换；aVR 与 aVL 互换 | `P_I < 0 AND R_I < S_I AND corr(II_obs, III_ref) > 0.9` |
| RA ↔ LL | II 导倒置；I 与 III 互换且倒置；aVR 与 aVF 互换 | `net_area(II) < 0 AND P_II < 0` |
| LA ↔ LL | I 与 II 互换；III 倒置；aVL 与 aVF 互换 | `corr(I_obs, II_ref) > 0.9 AND net_area(III) < 0` |
| RA ↔ RL(N) | II 导近似平线（振幅 < 100 μV） | `pp_amplitude(II) < 100 μV` |
| LL ↔ RL(N) | III 导近似平线 | `pp_amplitude(III) < 100 μV` |

#### 4.4.2 胸导反接检测

基于导联间模式互相关的递进分析：对 V1→V6 计算相邻导联中位搏的互相关系数序列 `c[k] = corr(Vk, Vk+1)`，正常情况下该序列平滑且各项 > 0.5。若某处出现显著低谷或递进逆转，则定位为该位置的导联互换。

同时校验 R 波振幅递进：正常 V1→V6 的 R 振幅单调上升至 V4–V5 后可略降，S 振幅单调下降。递进出现非单调跳变时标记可疑。

#### 4.4.3 与右位心的鉴别（关键）

LA↔RA 反接与右位心在肢导表现相同（I 导全倒置）。鉴别靠胸导：

| | I 导 | 胸导 R 波递进 |
|---|---|---|
| LA↔RA 反接 | 全倒置 | **正常递增** |
| 右位心 | 全倒置 | **V1→V6 振幅递减** |

判定式：
```
IF I导全倒置:
    IF R振幅(V6) < R振幅(V1) AND R振幅单调递减:
        → finding: DEXTROCARDIA
    ELSE:
        → finding: LEAD_SWAP_LA_RA
```

#### 4.4.4 反接检出后的动作

**不要自动纠正后继续判读。** 正确动作是：

1. 输出明确的反接警告陈述，置于报告最前
2. 终止后续诊断模块，或仅输出测量值不输出诊断结论
3. 提示重新采集

理由：自动纠正会掩盖采集操作问题，且若反接判断本身有误，会引入更大错误。

### 4.5 起搏信号预检

在未滤波支路上做一次快速扫描，检出起搏脉冲则设置 `PACED_CANDIDATE` 标志，供 L7 的 M0 模块使用（见 8.5、10.1）。

---

## 5. L3 波形检测与定界

定界精度决定整个系统的上限。测量不准，后面所有判据都是在错误数值上做正确推理。

### 5.1 算法选型

采用**二进小波变换模极大值法**（Martínez 等人的经典方案）作为主算法，Pan-Tompkins 作为第二检测器供 `bSQI` 与冗余校验使用。

选择理由：小波法天然多尺度，能在同一框架内处理 QRS（高频）、P/T（低频），且对基线漂移不敏感；纯确定性，无需训练。

### 5.2 小波配置

- 小波基：二次样条小波（quadratic spline wavelet），其一阶导数特性使波形拐点对应模极大值、波峰对应过零点
- 尺度：`2^1 … 2^5`（针对 `fs = 250 Hz` 定义；`fs = 500 Hz` 时整体上移一个尺度，即使用 `2^2 … 2^6`）
- 各尺度的主要用途：

| 尺度（fs=500Hz） | 主要能量对应 | 用途 |
|---|---|---|
| 2^2 | 40–125 Hz | 噪声评估、起搏钉 |
| 2^3 | 20–60 Hz | QRS 检测主尺度 |
| 2^4 | 10–30 Hz | QRS 边界、QRS 内部结构 |
| 2^5 | 5–15 Hz | T 波、P 波检测 |
| 2^6 | 2–8 Hz | T 波边界 |

### 5.3 R 峰检测

```
1. 在尺度 2^3、2^4 上计算小波系数 W3、W4
2. 阈值 T = k × RMS(W3, 全段)，k 初值 = 0.35
3. 在 W3 上寻找超过 T 的模极大值
4. 配对：寻找"正极大 → 负极大"（或反向）且间隔 < 120 ms 的模极大值对
5. 该对之间的过零点 = R 峰位置
6. 在 W4 上验证同一位置存在对应模极大值对（跨尺度一致性）
7. 不应期：两个 R 峰间隔 < 200 ms 时，保留幅值大者
8. 漏搏回溯：若某 RR > 1.5 × 局部中位 RR，在该区间内以 0.5×T 重新搜索
9. 多导联融合：见 5.7
```

**参数 `k` 的自适应**：初次检测后，用检出 R 峰的中位幅值重新设定 `T = 0.4 × median(peak_amplitude)`，重跑一次。两次结果不一致的心搏标记为低置信。

### 5.4 QRS 起终点定界

对每个 R 峰，在尺度 2^4 上：

```
QRS 起点：从 R 峰对应的第一个模极大值向前搜索，
          至 |W4| 降到该极大值的 ξ_on 倍（ξ_on = 0.05）
          或出现符号相反的模极大值为止
QRS 终点：从最后一个模极大值向后搜索，
          至 |W4| 降到 ξ_off 倍（ξ_off = 0.10）
```

`ξ_off > ξ_on` 是有意设计：QRS 终点后紧邻 ST 段，阈值过低会把 ST 起始段并入 QRS，造成 QRS 时限系统性偏长。

### 5.5 T 波检测与定界

```
1. 搜索窗：QRS 终点后 [100 ms, min(0.7×RR, 600 ms)]
   （窗口右界随 RR 自适应，心动过速时必须收窄）
2. 在尺度 2^5、2^6 上寻找该窗内最大模极大值对
3. 过零点 = T 峰
4. T 波极性：由模极大值对的符号顺序判定
   正向 T：负极大 → 正极大（对应上升-下降）
   负向 T：正极大 → 负极大
   双向 T：窗内存在两组显著模极大值对
5. T 终点定界：优先用小波阈值法（|W| 降至峰值的 0.125 倍）
   备选用切线法：过 T 波下降支最大斜率点作切线，与等电位线交点
```

**T 终点是全流程中误差最大的测量项**，直接决定 QT 与 QTc。建议同时实现小波法与切线法，两者差异 > 40 ms 时将 QT 标记为低置信。

### 5.6 P 波检测与定界

```
1. 搜索窗：QRS 起点前 [50 ms, min(0.35×RR, 250 ms)]
2. 在尺度 2^5、2^6 上寻找模极大值对，阈值取 QRS 阈值的 0.15 倍
3. 形态分类：正向 / 负向 / 双向（存在两组反向模极大值对）
4. 起终点定界方法同 T 波
```

#### 5.6.1 未下传 P 波的补充搜索（必需）

阈值法在高度房室阻滞时会遗漏 RR 间期中段孤立出现的 P 波。必须实现二次搜索：

```
FOR 每个 RR 间期:
    IF 该区间内已检出 P 波数 == 1 AND RR > 1.5 × median(RR):
        在该区间的非搜索窗区域，用 P 波模板做匹配搜索
        模板 = 该导联已确认 P 波的平均形态
        候选点须同时满足:
            (a) 时限 ≥ 60 ms
            (b) 振幅 > 5% × 相邻 QRS 振幅
            (c) 与模板极性一致（正/负/双向同类）
            (d) 与模板互相关系数 > 0.7
        → 标记为"未下传 P 波"
```

**这一步是二度 II 型与三度房室阻滞能否被检出的决定性环节。** 未实现该逻辑的系统会把三度房室阻滞误判为交界性逸搏心律。

### 5.7 多导联融合规则

单导联定界结果通过以下规则合成全局基准点：

| 规则 | 内容 |
|---|---|
| 存在性确认 | 某波须在 **≥ 4 个可用导联**的同一时刻（±20 ms）被检出，方视为存在 |
| 全局起点 | 取所有支持导联中**最早**的起点 |
| 全局终点 | 取所有支持导联中**最晚**的终点 |
| 时限计算 | 全局终点 − 全局起点 |

采用"最早起点 / 最晚终点"是国际通行做法，理由是心电向量在某些导联上的初始与终末部分可能与该导联轴垂直而不产生偏转。**若按单导联测量 QRS 时限，结果会系统性偏短 10–20 ms**，直接导致束支阻滞漏诊。

### 5.8 生理约束后处理

强制执行以下约束，违反者剔除：

1. 一个 RR 间期内最多一个 T 波；出现多个候选时，选择时间位置最接近 `QT_expected = 0.4 × sqrt(RR)` 的
2. P 波不得与 QRS 重叠
3. QRS 时限须在 [40 ms, 250 ms] 内，超界标记 `UNRELIABLE`
4. PR 间期须在 [80 ms, 500 ms] 内
5. 相邻心搏的 QRS 起点间隔须 ≥ 200 ms

### 5.9 验收标准

| 指标 | 数据集 | 目标 |
|---|---|---|
| QRS 检测 Se / +P | MIT-BIH Arrhythmia、NST | ≥ 99.5% / ≥ 99.5%（依 ANSI/AAMI EC57 协议） |
| P 波定界误差 | QT Database、LUDB | 起点/终点 SD ≤ 10 ms |
| QRS 定界误差 | 同上 | SD ≤ 8 ms |
| T 终点误差 | 同上 | SD ≤ 25 ms |

---

## 6. L4 心搏模板聚类与中位搏合成

### 6.1 目的

- 区分主导型心搏与异位心搏，异位心搏不得参与形态学测量
- 合成中位搏，作为所有振幅与时限测量的基准

### 6.2 模板聚类算法

```
1. 对齐：以 R 峰为基准，截取窗口 [-250 ms, +450 ms]
   （窗口须覆盖 P 波起点到 T 波终点）
2. 参与聚类的导联：选择 R 波振幅最大的 3 个导联，拼接为特征向量
3. 逐拍与已有模板计算 Pearson 相关系数：
       IF max(corr) ≥ 0.90:  归入该模板类，更新模板（滑动平均）
       ELSE:                 新建模板类
4. 类合并：若两模板互相关 ≥ 0.95，合并
5. 主导类 = 成员数最多的类
```

相关系数阈值 0.90 是经验值，需在验证集上标定。阈值过高会把噪声导致的形态波动拆成多类，过低会把室早并入主导类。

### 6.3 心搏类型判定（规则）

对每个模板类：

| 类型 | 判定条件 |
|---|---|
| 主导型（N） | 成员数最多的类 |
| 室性异位（V） | QRS 时限 > 120 ms **且** 与主导模板相关系数 < 0.80 **且** 无前导相关 P 波 **且** 联律间期 < 0.85 × 中位 RR |
| 室上性异位（S） | QRS 时限 ≤ 120 ms **且** 与主导模板相关系数 ≥ 0.80 **且** 有形态异常的前导 P' 波 **且** 联律间期 < 0.85 × 中位 RR |
| 融合波（F） | 联律间期介于室性与主导型之间，形态介于两者（与两个模板的相关系数均在 0.75–0.90） |
| 起搏搏动（P） | 检出起搏钉且钉后 40 ms 内出现 QRS |
| 逸搏 | 联律间期 > 1.15 × 中位 RR（**非**提前，而是延后） |

**联律间期与代偿间歇的判定**：
- 完全代偿间歇：`RR(前) + RR(后) ≈ 2 × 基础 RR`（容差 ±60 ms）→ 支持室性
- 不完全代偿间歇：`RR(前) + RR(后) < 2 × 基础 RR − 60 ms` → 支持房性

### 6.4 中位搏合成

```
1. 取主导类全部成员，按 QRS 起点对齐（不是 R 峰对齐 —— R 峰位置受形态影响）
   实际做法：先按 R 峰粗对齐，再用互相关做 ±10 ms 精对齐
2. 逐导联、逐采样点取中位数（不是均值 —— 中位数对异常点鲁棒）
3. 若主导类成员数 < 3，不合成中位搏，直接使用形态最典型的单拍，
   并设置标志 MEDIAN_BEAT_WEAK
```

### 6.5 等电位线基准

**所有振幅测量的零点基准必须统一定义为 PR 段**（P 波终点到 QRS 起点之间的中点附近 20 ms 窗的均值）。

若 P 波不可辨（如房颤），退化为使用 TP 段；若 TP 段也不可辨（心动过速），使用 QRS 起点前 20 ms 窗，并将 ST 测量标记为低置信。

**这是一个极易出错的实现点。** 不同基准会造成 ST 水平相差数十 μV，直接影响 STEMI 判定。必须在配置中显式声明基准策略，并在报告中可查。

### 6.6 形态复杂度门控

```
IF 主导类成员占比 < 30%  OR  模板类数 > 5:
    设置标志 MORPH_COMPLEX
    → 所有基于中位搏的形态学判据（肥大、缺血、束支阻滞）置 UNKNOWN
    → 仅输出节律相关结论
```

---

## 7. L5 特征量化 —— 特征字典

本章是实现清单。每一项均需在代码中有对应实现与单元测试。

### 7.1 全局时间参数

| ID | 名称 | 单位 | 计算方法 | 依赖 |
|---|---|---|---|---|
| `HR_mean` | 平均心率 | bpm | `60000 / mean(RR)`，仅计主导型心搏间的 RR | L3 |
| `HR_min` / `HR_max` | 最小/最大心率 | bpm | 基于 3 拍滑动窗 | L3 |
| `RR[]` | RR 间期序列 | ms | 逐拍 | L3 |
| `P_dur` | P 波时限 | ms | 多导联融合（5.7）| L3 |
| `PR_int` | PR 间期 | ms | P 全局起点 → QRS 全局起点 | L3 |
| `QRS_dur` | QRS 时限 | ms | 多导联融合 | L3 |
| `QT_int` | QT 间期 | ms | QRS 全局起点 → T 全局终点，取中位搏 | L4 |
| `QTcB` | Bazett 校正 QT | ms | `QT / sqrt(RR/1000)` | `QT_int`, `RR` |
| `QTcFri` | Fridericia 校正 | ms | `QT / (RR/1000)^(1/3)` | 同上 |
| `QTcFra` | Framingham 校正 | ms | `QT + 154 × (1 − RR/1000)` | 同上 |
| `QTcHod` | Hodges 校正 | ms | `QT + 1.75 × (HR − 60)` | 同上 |

**四种 QTc 全部计算并全部输出。** 临床阈值基于 Bazett 制定，但 Bazett 在 HR < 60 或 > 100 时失真显著。报告主值用 Bazett，同时给出 Fridericia 供参考；HR 超出 [50, 100] 时在报告中标注"Bazett 校正在此心率下可靠性下降"。

### 7.2 电轴与空间特征

| ID | 名称 | 单位 | 计算方法 |
|---|---|---|---|
| `P_axis` | P 波额面电轴 | ° | 基于 I、aVF 的 P 波**净面积**（不是振幅） |
| `QRS_axis` | QRS 额面电轴 | ° | 基于 I、aVF 的 QRS **净面积** |
| `T_axis` | T 波额面电轴 | ° | 基于 I、aVF 的 T 波净面积 |
| `QRST_angle_f` | 额面 QRS-T 夹角 | ° | `\|QRS_axis − T_axis\|`，规约到 [0, 180] |
| `transition_zone` | 胸导移行区 | — | R/S = 1 所在导联位置，可为分数（如 V3.5） |

电轴计算式（以 QRS 为例）：
```
A_I   = 中位搏 I 导 QRS 区间的净面积（正面积 − 负面积）
A_aVF = 中位搏 aVF 导 QRS 区间的净面积
QRS_axis = atan2(A_aVF, A_I) × 180 / π   # 结果落在 [-180, 180]
```

**必须用净面积而非 R−S 振幅差。** 振幅法在多相 QRS（rSR'、qRs）上误差可达 30°，会造成分支阻滞的误判。

电轴分区：

| 范围 | 判定 |
|---|---|
| −30° ~ +90° | 正常 |
| −30° ~ −90° | 电轴左偏 |
| +90° ~ +180° | 电轴右偏 |
| −90° ~ −180° | 电轴极度偏移（"无人区"） |

### 7.3 逐导联形态学特征

对 12 个导联中的每一个，在中位搏上计算：

#### P 波

| ID | 说明 |
|---|---|
| `P_amp_pos[l]` | 正向分量峰值（μV） |
| `P_amp_neg[l]` | 负向分量峰值（μV，取绝对值） |
| `P_dur_lead[l]` | 该导联 P 波时限 |
| `P_morph[l]` | 形态编码：`POS` / `NEG` / `BIPHASIC_PN` / `BIPHASIC_NP` / `NOTCHED` |
| `PTFV1` | V1 导 P 终末电势 = 负向分量深度(mm) × 时限(s)，单位 mm·s |

#### QRS

| ID | 说明 |
|---|---|
| `Q_amp[l]`, `Q_dur[l]` | Q 波振幅与时限 |
| `R_amp[l]`, `R_dur[l]` | R 波 |
| `S_amp[l]` | S 波振幅（绝对值） |
| `Rp_amp[l]` | R' 波振幅（若存在） |
| `Sp_amp[l]` | S' 波振幅（若存在） |
| `RS_ratio[l]` | `R_amp / S_amp` |
| `VAT[l]` | R 峰时间（本位曲折）= QRS 起点 → 最后一个 R 峰顶 |
| `QRS_morph[l]` | 形态编码，见 7.3.1 |
| `QRS_notch[l]` | 切迹计数与位置（QRS 时限的百分位） |
| `QRS_amp_pp[l]` | QRS 峰峰值 |

#### ST-T

| ID | 说明 |
|---|---|
| `ST_J[l]` | J 点相对等电位线的偏移（μV），J 点 = QRS 终点 |
| `ST_40[l]`, `ST_60[l]`, `ST_80[l]` | J 点后 40/60/80 ms 处偏移 |
| `ST_slope[l]` | J 点到 J+80ms 的斜率（μV/ms） |
| `ST_shape[l]` | `UPSLOPING` / `HORIZONTAL` / `DOWNSLOPING` / `CONCAVE` / `CONVEX` |
| `T_amp[l]` | T 波峰值（带符号） |
| `T_polarity[l]` | `POS` / `NEG` / `BIPHASIC` / `FLAT`（\|T_amp\| < 100 μV） |
| `T_symmetry[l]` | 上升支时长 / 下降支时长 |
| `Tpe[l]` | T 峰到 T 终点间期（ms） |
| `U_amp[l]` | U 波振幅（若可辨） |

**ST 测量点的选择依心率**：HR < 100 用 `ST_60`；HR ≥ 100 用 `ST_40`（心率快时 T 波前移，J+60/80 已落入 T 波）。所有 STEMI 判据用 `ST_J`，其余缺血判据用心率自适应点。

#### 7.3.1 QRS 形态编码规则

按波的顺序与相对大小编码。大写表示振幅 ≥ 500 μV，小写表示 < 500 μV：

```
Q/q : QRS 起始的负向波（其前无正向波）
R/r : 正向波
S/s : R 波之后的负向波
R'/r', S'/s' : 第二组正负向波
QS  : 全负向、无任何正向成分
```
示例编码：`qR`、`rS`、`RS`、`rSR'`、`QS`、`Rs`、`qRs`、`rsR's'`

判定顺序：先做波的分割（以过等电位线为界，且单个波振幅须 > 50 μV 才计入），再按上述规则命名。

### 7.4 节律与变异性特征

| ID | 计算方法 |
|---|---|
| `RR_mean`, `RR_SD` | 主导型心搏 RR 的均值与标准差 |
| `RR_CV` | `RR_SD / RR_mean` |
| `RMSSD` | `sqrt(mean((RR[i+1] − RR[i])^2))` |
| `pNN50` | \|RR[i+1] − RR[i]\| > 50 ms 的比例 |
| `SD1`, `SD2` | Poincaré 图短轴/长轴：`SD1 = SDSD/√2`，`SD2 = sqrt(2×RR_SD² − SD1²)` |
| `RR_entropy` | RR 增量序列的香农熵（分箱宽度 20 ms） |
| `P_presence_ratio` | 检出 P 波的心搏占比 |
| `PP_regularity` | PP 间期的变异系数（仅在 P 可辨时） |
| `AV_ratio` | P 波数 : QRS 数 |
| `PR_variability` | PR 间期的标准差与趋势斜率（Wenckebach 检测用） |
| `f_wave_freq` | TQ 段信号的功率谱主频（3–12 Hz 内） |
| `f_wave_regularity` | 主频谱峰的尖锐度（峰值 / 邻域均值） |
| `PVC_count`, `PAC_count` | 各类异位搏数 |
| `couplet_run` | 连续异位搏的最长游程 |
| `QRS_alternans` | 奇偶拍 QRS 振幅差的均值 / 平均振幅 |

#### 7.4.1 f 波分析实现要点

```
1. 提取所有 TQ 段（T 终点 → 下一 QRS 起点），要求段长 ≥ 100 ms
2. 拼接后做零填充 FFT，频率分辨率 ≤ 0.1 Hz
3. 在 3–12 Hz 范围寻找主峰
4. 房扑判定支持：主峰在 4.0–5.7 Hz（240–340 次/分）且尖锐度 > 4
5. 房颤判定支持：3–9 Hz 内无尖锐主峰，谱形宽平
```

TQ 段过短（心率快）时该特征置 `UNKNOWN`。

### 7.5 派生诊断指数

| ID | 计算式 | 说明 |
|---|---|---|
| `SokolowLyon` | `S_amp[V1] + max(R_amp[V5], R_amp[V6])` | μV |
| `Cornell_V` | `R_amp[aVL] + S_amp[V3]` | μV |
| `Cornell_P` | `(R_amp[aVL] + S_amp[V3] + 女性?600μV:0) × QRS_dur` | μV·ms |
| `Peguero` | `max(所有导联最深 S) + S_amp[V4]` | μV |
| `RomhiltEstes` | 见 10.3.3 积分表 | 分 |
| `RVH_index` | `R_amp[V1] + max(S_amp[V5], S_amp[V6])` | μV |
| `Sgarbossa_score` | 见 10.4.5 | 分 |
| `Smith_ratio[l]` | `ST_J[l] / S_amp[l]`（同导联，带符号） | 无量纲 |
| `ST_max_elev` | 全导联 `ST_J` 最大正值及其导联 | μV |
| `ST_max_depr` | 全导联 `ST_J` 最大负值及其导联 | μV |

### 7.6 缺失值约定

任一特征在下列情况置 `NULL`：
- 所依赖的波未被检出
- 所依赖的导联被 L2 判为不可用
- 中位搏合成失败（`MEDIAN_BEAT_WEAK` 且成员数 < 2）

`NULL` 参与运算的结果为 `NULL`，不得用 0 或均值填充。

---

## 8. L6 发现层

发现层把连续测量离散化为带证据的语义项。**L7 的规则只允许消费 finding，不允许直接读取 L5 的数值。**

### 8.1 数据结构

```
Finding {
    id:        string          # 如 "PR_PROLONGED"
    value:     TRUE|FALSE|UNKNOWN
    grade:     optional enum   # 如 MILD / MODERATE / SEVERE
    evidence:  [ { lead, metric, measured, threshold, clause } ]
    confidence: HIGH|MEDIUM|LOW
}
```

`confidence` 的降级规则：
- 测量值落在阈值 ±10% 区间内 → 降至 MEDIUM
- 依赖导联的 SQI 处于边界 → 降一级
- 设备滤波元数据提示可能失真（见 2.5）→ 降一级
- `MEDIAN_BEAT_WEAK` → 所有形态学 finding 降至 LOW

### 8.2 时间参数类 finding

| Finding ID | 判定式 | 分层 |
|---|---|---|
| `HR_BRADY` | `HR_mean < 60` | |
| `HR_TACHY` | `HR_mean > 100` | |
| `PR_PROLONGED` | `PR_int > 200`（且在 ≥ 80% 的下传心搏成立） | |
| `PR_SHORT` | `PR_int < 120` | |
| `PR_PROGRESSIVE` | PR 呈单调递增且总增幅 > 40 ms | Wenckebach |
| `PR_CONSTANT` | PR 的 SD < 10 ms | |
| `QRS_WIDE` | `QRS_dur ≥ 120` | |
| `QRS_BORDERLINE` | `110 ≤ QRS_dur < 120` | 不完全性阻滞用 |
| `QT_PROLONGED` | `QTcB > 450`（男）/ `> 460`（女） | 按性别 |
| `QT_SEVERE` | `QTcB > 500` | |
| `QT_SHORT` | `QTcB < 340` | |
| `P_DUR_PROLONGED` | `P_dur ≥ 120` | |

### 8.3 电轴类 finding

| Finding ID | 判定式 |
|---|---|
| `AXIS_NORMAL` | `−30 ≤ QRS_axis ≤ 90` |
| `AXIS_LEFT` | `−90 ≤ QRS_axis < −30` |
| `AXIS_RIGHT` | `90 < QRS_axis ≤ 180` |
| `AXIS_EXTREME` | `−180 ≤ QRS_axis < −90` |
| `POOR_R_PROGRESSION` | `R_amp[V3] < 300 μV` 或 移行区晚于 V4 |
| `EARLY_TRANSITION` | 移行区早于 V2 |

### 8.4 形态学类 finding（节选，完整表见附录 A）

| Finding ID | 判定式 |
|---|---|
| `V1_QS_OR_rS` | `QRS_morph[V1] ∈ {QS, rS}` |
| `LATERAL_NOTCHED_R` | `I, aVL, V5, V6` 中 ≥ 2 个导联存在 QRS 中段切迹 |
| `NO_Q_LATERAL` | `Q_amp[I] < 50 AND Q_amp[V5] < 50 AND Q_amp[V6] < 50` |
| `VAT_LATERAL_LONG` | `VAT[V5] > 60 OR VAT[V6] > 60` |
| `V1_RSR_PATTERN` | `QRS_morph[V1] ∈ {rsR', rSR', rsr'}` |
| `WIDE_S_LATERAL` | `S_dur[I] > 40 AND S_dur[V6] > 40` |
| `PATHOLOGIC_Q[region]` | 见 10.5.1 |
| `DELTA_WAVE` | QRS 起始 60 ms 内斜率显著低于后续（起始钝挫），且 `PR_SHORT` |

### 8.5 起搏相关 finding

| Finding ID | 判定式 |
|---|---|
| `PACING_SPIKE_PRESENT` | 未滤波支路上检出脉冲：宽度 < 2 ms，斜率 > 5000 μV/ms，且在 ≥ 2 个导联同时出现 |
| `PACED_VENTRICULAR` | 钉后 40 ms 内出现宽 QRS（≥ 120 ms） |
| `PACED_ATRIAL` | 钉后 40 ms 内出现 P 波 |
| `PACED_DUAL` | 同一心动周期内检出两个钉，间隔 80–300 ms |

起搏钉检测的实现要点：使用未滤波信号的一阶差分，阈值须自适应（取全段差分幅值的 99.9 分位数的 0.5 倍）。**注意与高频噪声尖峰的区分**：起搏钉在多导联同时出现且极性符合导联轴关系，噪声尖峰通常仅在单导联出现。

### 8.6 节律类 finding

| Finding ID | 判定式 |
|---|---|
| `RR_IRREGULAR_ABSOLUTE` | `RR_CV > 0.15` **且** `RR_entropy > 阈值` **且** Poincaré 呈弥散分布（`SD1/SD2 > 0.4`） |
| `RR_REGULAR` | `RR_CV < 0.05` |
| `RR_REGULARLY_IRREGULAR` | RR 序列存在周期性模式（自相关在 lag 2–4 处有显著峰） |
| `P_ABSENT` | `P_presence_ratio < 0.2` |
| `P_UNIFORM` | 各 P 波互相关 > 0.85 且 `P_axis` 在 0°~75° |
| `P_MULTIFORM` | 存在 ≥ 3 种形态不同的 P 波 |
| `F_WAVE_PRESENT` | `f_wave_freq ∈ [4.0, 5.7] Hz` 且 `f_wave_regularity > 4` |
| `AV_DISSOCIATION` | `PP_regularity < 0.1` 且 `RR_CV < 0.1` 且 PR 间期无固定关系（PR 的 SD > 60 ms） |

---

## 9. L7 执行顺序与门控 DAG

### 9.1 为什么必须有执行顺序

规则系统最常见的架构错误是把 N 个诊断实现为 N 个独立的二分类判定。实际上诊断之间存在强依赖：某些诊断成立会**使另一些诊断的标准判据完全失效**。例如 LBBB 存在时，常规 ST 段偏移判据不能用于判断缺血，必须切换到 Sgarbossa 判据。

因此 L7 必须按固定拓扑顺序执行，上游结果作为下游的门控条件。

### 9.2 门控 DAG

```
                        ┌─────────────────────┐
                        │ 入口：L6 findings   │
                        └──────────┬──────────┘
                                   ▼
          ┌────────────────────────────────────────────┐
   [M0]   │ 起搏与设备识别                              │
          │  PACING_SPIKE_PRESENT ?                    │
          └───────┬────────────────────────┬───────────┘
              是  │                        │ 否
                  ▼                        ▼
      ┌───────────────────────┐  ┌──────────────────────────┐
      │ 关闭：M3 全部          │  │ [M2] 心室内传导           │
      │ 关闭：M4 常规 ST 判据  │  │  QRS 宽度分类             │
      │ 开启：M4-Sgarbossa 支路│  │  → 预激 / LBBB / RBBB /   │
      │ 开启：M1（起搏节律分型）│  │    LAFB / LPFB / IVCD     │
      └───────────────────────┘  └──────┬───────────────────┘
                                        ▼
              ┌──────────────────────────────────────────────┐
              │ 预激成立？→ 关闭 M3、关闭 M4 全部、           │
              │             关闭病理性 Q 波判定                │
              │ LBBB 成立？→ 关闭 M3-LVH、关闭 M4 常规 ST，   │
              │             切换 M4 至 Sgarbossa 支路          │
              │ RBBB 成立？→ M3-LVH 切换至 RBBB 专用判据集，  │
              │             置信度降一级；V1–V3 的 ST-T 改变   │
              │             按继发性处理                        │
              └──────────────────┬───────────────────────────┘
                                 ▼
                      ┌──────────────────────┐
               [M1]   │ 节律与房室传导         │
                      │  基础节律 → 异位搏     │
                      │  → 房室阻滞分度        │
                      └──────────┬───────────┘
                                 ▼
                      ┌──────────────────────┐
               [M3]   │ 心房异常与心室肥大     │  ← 受 M0/M2 门控
                      └──────────┬───────────┘
                                 ▼
                      ┌──────────────────────┐
               [M4]   │ 缺血与心肌梗死         │  ← 受 M0/M2 门控
                      └──────────┬───────────┘
                                 ▼
                      ┌──────────────────────┐
               [M5]   │ 复极异常 / 电解质 / 药物│
                      └──────────┬───────────┘
                                 ▼
                      ┌──────────────────────┐
               [M6]   │ 其他（低电压、电交替、  │
                      │  心包炎、右位心等）     │
                      └──────────────────────┘
```

### 9.3 M2 先于 M1 的理由

M2（心室内传导）在 M1（节律）之前执行，理由是：宽 QRS 心动过速的鉴别需要知道基础状态下的束支阻滞形态。

**但存在一个循环依赖**：若整份记录全部为宽 QRS 心动过速，则不存在可供 M2 分析的室上性心搏。处理方式：

```
IF 主导型心搏均为宽 QRS AND HR > 100:
    跳过 M2 的束支阻滞判定
    直接进入 M1 的宽 QRS 心动过速鉴别流程
    M3、M4 全部关闭
```

### 9.4 门控矩阵（实现时应作为配置表）

| 上游条件 | M1 | M2 | M3-LAE/RAE | M3-LVH | M3-RVH | M4 常规 | M4-Sgarbossa | M5 | M6 |
|---|---|---|---|---|---|---|---|---|---|
| 心室起搏 | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ | 部分 |
| 心房起搏 | ✓ | ✓ | ✗ | ✓ | ✓ | ✓ | — | ✓ | ✓ |
| 预激 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | 部分 |
| LBBB | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ | ✓ | 部分 | ✓ |
| RBBB | ✓ | ✓ | ✓ | 降级 | ✗ | 部分 | — | ✓ | ✓ |
| MORPH_COMPLEX | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | 部分 |
| 房颤 | ✓ | ✓ | ✗（无P波） | ✓ | ✓ | ✓ | — | ✓ | ✓ |

图例：✓ 执行 ｜ ✗ 关闭（输出 `UNKNOWN` 而非 `FALSE`）｜ 降级 = 执行但置信度降一级 ｜ 部分 = 仅部分判据可用

---

## 10. L7 诊断规则库

### 10.1 M0 —— 起搏与设备识别

| 陈述 | 判定条件 |
|---|---|
| 心室起搏心律 | `PACED_VENTRICULAR` 且起搏搏动占比 > 80% |
| 心房起搏心律 | `PACED_ATRIAL` 且起搏搏动占比 > 80% |
| 双腔起搏心律 | `PACED_DUAL` |
| 间歇性起搏 | 起搏搏动占比 20–80% |
| 起搏失夺获（提示） | 检出起搏钉但其后 40 ms 内无对应除极波，出现 ≥ 2 次 |
| 起搏感知不良（提示） | 起搏钉出现在自身 QRS 后 40–200 ms 内 |

起搏相关的失夺获/感知异常仅输出为提示，并强制人工复核标志。

### 10.2 M2 —— 心室内传导

#### 10.2.1 心室预激（WPW 型）

必须**全部**满足：

| # | 条件 |
|---|---|
| 1 | `PR_int < 120 ms` |
| 2 | `QRS_dur ≥ 110 ms` |
| 3 | `DELTA_WAVE`：QRS 起始 40–60 ms 段的平均斜率 < 后续 QRS 段平均斜率的 50%，且该钝挫在 ≥ 2 个导联可见 |
| 4 | 存在继发性 ST-T 改变（ST-T 方向与 QRS 终末向量相反） |

成立时输出"心室预激"，并**强制关闭 M3、M4 全部判据**，报告中注明"预激图形，无法评估心肌梗死与心室肥大"。

#### 10.2.2 完全性左束支阻滞（LBBB）

| # | 条件 |
|---|---|
| 1 | `QRS_dur ≥ 120 ms` |
| 2 | `V1_QS_OR_rS`：V1、V2 呈 QS 或 rS（r 波振幅 < 200 μV） |
| 3 | I、aVL、V5、V6 呈宽大单向 R 波，且 ≥ 2 个导联有中段切迹或顿挫（`LATERAL_NOTCHED_R`） |
| 4 | `NO_Q_LATERAL`：I、V5、V6 无 q 波（aVL 允许存在窄 q） |
| 5 | `VAT_LATERAL_LONG`：V5 或 V6 的 R 峰时间 > 60 ms |
| 6 | ST-T 与 QRS 主波方向相反 |

条件 1–5 全部满足 → "完全性左束支阻滞"（HIGH）
条件 1–4 满足、第 5 项 `UNKNOWN` → 输出但降至 MEDIUM

**严格标准（Strauss，仅在配置开启时输出附加陈述）**：
- `QRS_dur ≥ 140 ms`（男）/ `≥ 130 ms`（女）
- V1、V2 呈 QS 或 rS
- I、aVL、V1、V2、V5、V6 中 ≥ 2 个导联出现 QRS 中段切迹或顿挫

该标准用于心脏再同步治疗候选者筛选，敏感度低于常规标准但特异度高。

#### 10.2.3 完全性右束支阻滞（RBBB）

| # | 条件 |
|---|---|
| 1 | `QRS_dur ≥ 120 ms` |
| 2 | `V1_RSR_PATTERN`：V1 或 V2 呈 rsr'、rsR'、rSR'，通常 R' > r |
| 3 | `WIDE_S_LATERAL`：I 与 V6 存在宽 S 波（时限 > 40 ms 且 S 时限 > R 时限） |
| 4 | `VAT[V1] > 50 ms` |

条件 1、2、3 为必需；条件 4 为支持。

#### 10.2.4 不完全性束支阻滞

`QRS_BORDERLINE`（110–119 ms）且形态学条件满足 → 输出"不完全性左/右束支阻滞"。

#### 10.2.5 左前分支阻滞（LAFB）

| # | 条件 |
|---|---|
| 1 | `QRS_axis ∈ [−90°, −45°]` |
| 2 | aVL 呈 qR |
| 3 | `VAT[aVL] ≥ 45 ms` |
| 4 | II、III、aVF 呈 rS |
| 5 | `QRS_dur < 120 ms`（除非合并 RBBB） |

**排除条件**（任一成立则不输出）：下壁心肌梗死、心室预激、心室肥大导致的电轴左偏。

#### 10.2.6 左后分支阻滞（LPFB）

| # | 条件 |
|---|---|
| 1 | `QRS_axis ∈ [+90°, +180°]` |
| 2 | I、aVL 呈 rS |
| 3 | II、III、aVF 呈 qR |
| 4 | `QRS_dur < 120 ms` |

**排除条件**：右心室肥大、侧壁心肌梗死、慢性肺部疾病、瘦长体型（体型信息通常不可得）。

由于排除条件多数依赖临床信息而非心电图，LPFB **一律输出为"可能"级别**并附"需结合临床与体型评估"。

#### 10.2.7 非特异性室内传导延迟（IVCD）

`QRS_dur ≥ 120 ms` 且不满足 LBBB 亦不满足 RBBB 的形态学条件。

### 10.3 M3 —— 心房异常与心室肥大

#### 10.3.1 心房异常

| 陈述 | 条件（满足任一） |
|---|---|
| 左房异常 | `P_dur[II] ≥ 120 ms`；或 `PTFV1 ≥ 0.04 mm·s`（V1 负向分量深度 ≥ 100 μV 且时限 ≥ 40 ms） |
| 右房异常 | `P_amp[II] ≥ 250 μV`；或 V1 的 P 波起始正向分量 ≥ 150 μV |
| 双房异常 | 上述两者同时成立 |

门控：`P_ABSENT` 为 TRUE（房颤等）时全部置 `UNKNOWN`。

#### 10.3.2 左心室肥大 —— 多判据并行评估

**必须并行计算全部判据后融合，不得只用单一判据。** AHA/ACCF/HRS 明确指出各判据敏感度普遍低于 50%、特异度约 85%–90%，且不同判据之间差异显著——在一大组轻中度高血压患者中，仅约 11% 的 LVH 患者同时满足 Cornell 电压与 Sokolow-Lyon 两个标准。

| 判据 | 阈值 |
|---|---|
| Sokolow-Lyon | `S_amp[V1] + max(R_amp[V5], R_amp[V6]) ≥ 3500 μV` |
| Cornell 电压 | `R_amp[aVL] + S_amp[V3] > 2800 μV`（男）/ `> 2000 μV`（女） |
| Cornell 乘积 | `(R_amp[aVL] + S_amp[V3] + 女性?600:0) × QRS_dur > 244000 μV·ms` |
| Peguero-Lo Presti | `max(全导联最深 S) + S_amp[V4] ≥ 2300 μV`（男）/ `≥ 1900 μV`（女） |
| Romhilt-Estes | 见 10.3.3，≥ 5 分 |
| aVL 单导 | `R_amp[aVL] ≥ 1100 μV` |
| 肢导电压 | 任一肢导 `QRS_amp_pp ≥ 2000 μV` |

**融合规则**：

```
n = 阳性判据数量
strain = 存在继发性 ST-T 改变（见下）

IF Romhilt_Estes ≥ 5 OR n ≥ 2:
    → "左心室肥大"                    confidence = HIGH
ELIF n == 1 AND strain:
    → "左心室肥大"                    confidence = MEDIUM
ELIF n == 1:
    → "左心室高电压"（不下肥大结论）   confidence = MEDIUM
ELIF Romhilt_Estes == 4:
    → "可能左心室肥大"                confidence = LOW
ELSE:
    → FALSE
```

**继发性 ST-T 改变（"劳损"型）的判定式**：在以 R 波为主的导联（V5、V6、I、aVL）中，`ST_J < −50 μV` 且 ST 呈下斜型（`ST_slope < 0`）且 T 波非对称性倒置（`T_amp < 0` 且 `T_symmetry < 0.8`）。

#### 10.3.3 Romhilt-Estes 积分表

| 项目 | 分值 |
|---|---|
| 任一肢导 R 或 S 振幅 ≥ 2000 μV，或 `S_amp[V1或V2] ≥ 3000 μV`，或 `R_amp[V5或V6] ≥ 3000 μV` | 3 |
| 继发性 ST-T 改变，**未**使用洋地黄 | 3 |
| 继发性 ST-T 改变，使用洋地黄 | 1 |
| 左房异常（`PTFV1 ≥ 0.04 mm·s`） | 3 |
| 电轴左偏 ≤ −30° | 2 |
| `QRS_dur ≥ 90 ms` | 1 |
| `VAT[V5或V6] ≥ 50 ms` | 1 |

≥ 5 分：左心室肥大 ｜ 4 分：可能左心室肥大

注：洋地黄使用信息通常不可得。实现时将该字段设为可选输入，缺失时按"未使用"计分并在证据链中标注该假设。

#### 10.3.4 门控（关键）

| 上游状态 | 处理 |
|---|---|
| LBBB | **禁止**输出 LVH 相关结论。AHA 立场是 LBBB 存在时不应尝试诊断 LVH。输出"存在左束支阻滞，无法评估心室肥大" |
| RBBB | 切换至 RBBB 专用判据集：`S_amp[V1] > 200 μV`、`R_amp[V5或V6] > 1500 μV`、`QRS_axis < −30°`。这组判据敏感度约 46%–68%、特异度约 57%–71%，故置信度强制降至 LOW，措辞用"可能" |
| 心室起搏 | 禁止输出 |
| 预激 | 禁止输出 |

#### 10.3.5 右心室肥大

| # | 条件 |
|---|---|
| 1 | `RS_ratio[V1] > 1` 且 `R_amp[V1] ≥ 700 μV` |
| 2 | `R_amp[V1] + max(S_amp[V5], S_amp[V6]) ≥ 1050 μV` |
| 3 | `QRS_axis > +90°`（严格：`≥ +110°`） |
| 4 | `RS_ratio[V5或V6] ≤ 1` |
| 5 | V1–V3 存在继发性 ST-T 改变 |

≥ 2 项满足 → "右心室肥大"（MEDIUM）；≥ 3 项 → HIGH

**排除条件**：后壁心肌梗死（可致 V1 高 R）、RBBB、B 型预激、右位心、导联反接。任一成立时置 `UNKNOWN`。

### 10.4 M4 —— 缺血与心肌梗死

#### 10.4.1 导联解剖分组与相邻关系

```
下壁    : II, III, aVF
前间壁  : V1, V2
前壁    : V3, V4
前侧壁  : V5, V6
高侧壁  : I, aVL
后壁    : 由 V1–V3 镜像推断（或 V7–V9 直接记录）
右室    : V3R, V4R（需额外导联）
```

"相邻两导联"的定义：
- 胸导相邻链：`V1–V2–V3–V4–V5–V6`
- 肢导相邻链（Cabrera 序列）：`aVL – I – (−aVR) – II – aVF – III`

**实现注意**：III 与 aVF 相邻，aVF 与 II 相邻，但 III 与 II **不相邻**。按导联在心电图纸上的排列顺序判断相邻是错误的。

#### 10.4.2 ST 段抬高阈值表

依第四版心肌梗死通用定义，测量点为 **J 点**（`ST_J`），需**相邻两个导联同时满足**：

| 导联 | 性别/年龄 | 阈值 |
|---|---|---|
| V2, V3 | 男 ≥ 40 岁 | ≥ 200 μV |
| V2, V3 | 男 < 40 岁 | ≥ 250 μV |
| V2, V3 | 女（任何年龄） | ≥ 150 μV |
| 其余标准导联 | 任何 | ≥ 100 μV |
| V3R, V4R | 男 < 30 岁 | ≥ 100 μV |
| V3R, V4R | 其他 | ≥ 50 μV |
| V7–V9 | 任何 | ≥ 50 μV |

年龄或性别缺失时，V2/V3 的判定置 `UNKNOWN`（见 2.2 策略选择）。

#### 10.4.3 ST 段压低与 T 波倒置

| 项 | 阈值 |
|---|---|
| ST 压低 | 相邻两导联 `ST_J ≤ −50 μV`，且 ST 呈水平型或下斜型（`ST_slope ≤ 0`） |
| T 波倒置 | 相邻两导联 `T_amp ≤ −100 μV`，且该导联 `RS_ratio > 1` |

上斜型 ST 压低不计入（生理性常见，尤其心动过速时）。

#### 10.4.4 高危形态学模板

以下模板用于识别典型阈值判据会漏检的急性冠脉闭塞表现。**规则化实现的敏感度有限，全部输出为"提示"级并强制人工复核标志。**

**(a) de Winter 型**
```
count over V1..V6 where:
    ST_J[l] ∈ [−300, −100] μV
    AND ST_slope[l] > 1.0 μV/ms   (上斜型)
    AND T_amp[l] > 500 μV          (高尖)
    AND T_symmetry[l] ∈ [0.8, 1.2] (对称)
≥ 4
AND ST_J[aVR] ≥ 0
→ "提示前降支近段闭塞（de Winter 型改变）"
```

**(b) Wellens B 型**
```
T_amp[V2..V4] ≤ −200 μV （≥2 个导联）
AND T_symmetry ∈ [0.85, 1.15]
AND R_amp[V3] > 300 μV        (R 波保留)
AND ST_J[V2..V4] < 100 μV     (无显著抬高)
AND NOT PATHOLOGIC_Q[前壁]
→ "提示前降支近段严重狭窄（Wellens 型改变）"
```

**(c) Wellens A 型**：V2–V4 的 `T_morph` 为双向（先正后负），其余同上。

**(d) 左主干 / 三支病变型**
```
ST_J[aVR] ≥ 100 μV
AND count(全导联 ST_J ≤ −100 μV) ≥ 6
AND ST_J[aVR] > ST_J[V1]
→ "提示左主干或三支病变，广泛心内膜下缺血"
```

**(e) 后壁心肌梗死**
```
count(V1..V3 where ST_J ≤ −50 μV) ≥ 2
AND RS_ratio[V2] > 1
AND R_dur[V2] ≥ 40 ms
AND T_amp[V2] > 0
→ "提示后壁心肌梗死，建议加做 V7–V9"
```

**(f) 终末 QRS 变形**：在 ST 抬高的导联中，QRS 终末既无 S 波亦无 J 点切迹 → 提示急性完全闭塞，提升优先级。

#### 10.4.5 LBBB 或心室起搏下的急性心肌梗死判定

常规 ST 判据在 LBBB 与心室起搏下完全失效（继发性 ST-T 改变本身就表现为显著偏移）。此时切换至 Sgarbossa 支路。

**原始 Sgarbossa 评分**

| 条件 | 分值 |
|---|---|
| 与 QRS 主波**同向**的 ST 抬高 ≥ 100 μV（≥ 1 个导联） | 5 |
| V1–V3 中与 QRS 主波**同向**的 ST 压低 ≥ 100 μV | 3 |
| 与 QRS 主波**不同向**的 ST 抬高 ≥ 500 μV | 2 |

总分 ≥ 3 → 提示急性心肌梗死（特异度高，敏感度低）

**改良 Smith 判据（推荐同时实现）**

将第三条替换为比值判据：
```
对每个不一致导联 l（ST 抬高方向与 QRS 主波方向相反）:
    ratio = ST_J[l] / S_amp[l]        # 两者符号相反，比值为负
    IF ratio ≤ −0.25:  阳性
```
即 ST 抬高幅度 ≥ 该导联 S 波深度的 25%。该判据的敏感度显著优于原始第三条。

第一、二条保留不变。任一条阳性即输出提示。

### 10.5 病理性 Q 波与心肌梗死定位

#### 10.5.1 病理性 Q 波判定

| 导联 | 条件 |
|---|---|
| V2, V3 | 任何 `Q_dur ≥ 20 ms`，或呈 QS 形态 |
| I, II, aVL, aVF, V4, V5, V6 | **相邻两导联** `Q_dur ≥ 30 ms` 且 `Q_amp ≥ 100 μV`，或呈 QS 形态 |
| V1, V2（后壁等价） | `R_dur ≥ 40 ms` 且 `RS_ratio ≥ 1` 且 T 波直立 |

**aVR 不参与病理性 Q 波判定**（aVR 正常即可呈 QS）。
**III 导单独的 Q 波不足以判定**，必须有 II 或 aVF 伴随。

#### 10.5.2 排除条件

任一成立时，全部病理性 Q 波判定置 `UNKNOWN`：

- 完全性左束支阻滞
- 心室预激
- 心室起搏
- 左心室肥大（可致 V1–V3 呈 QS）
- 右位心 / 导联反接
- `MORPH_COMPLEX`

#### 10.5.3 定位与分期

| 区域 | 导联 |
|---|---|
| 前间壁 | V1, V2 |
| 前壁 | V3, V4 |
| 前侧壁 | V5, V6 |
| 高侧壁 | I, aVL |
| 广泛前壁 | V1–V6 (+ I, aVL) |
| 下壁 | II, III, aVF |
| 后壁 | V1–V3 镜像 |

分期规则（仅基于单份心电图，可靠性有限）：

```
IF 病理性Q波 AND 该区域 ST 抬高:
    → "心肌梗死，急性期改变"（优先级：急症）
ELIF 病理性Q波 AND 该区域 T 波倒置 AND 无 ST 抬高:
    → "心肌梗死，演变期改变"
ELIF 病理性Q波 AND ST-T 正常:
    → "陈旧性心肌梗死（或既往梗死）"
```

**必须标注**："心电图无法可靠判定梗死时间，分期结论需结合临床与既往心电图对照。"

### 10.6 M1 —— 节律与房室传导

#### 10.6.1 基础节律决策树

```
1. 起搏搏动占比 > 80% ?
      → 起搏心律（分型见 10.1），结束

2. 心房活动分类:
   a) P 波均一、P_axis ∈ [0°, 75°]、每 P 后有 QRS、PR 恒定
        → 窦性来源
   b) P 波形态异常但均一、心房率 100–250、规整
        → 房性心动过速
   c) ≥ 3 种 P 形态 + RR 不齐 + HR > 100
        → 多源性房性心动过速
   d) F_WAVE_PRESENT（心房率 240–340、规整锯齿）
        → 心房扑动
   e) P_ABSENT + RR_IRREGULAR_ABSOLUTE + f 波谱宽平
        → 心房颤动
   f) P 波倒置于 II/III/aVF 且 PR < 120 ms，或 P 缺如而 QRS 窄
        → 交界性来源

3. 窦性来源者按心率细分:
      HR < 60  → 窦性心动过缓
      HR > 100 → 窦性心动过速
      RR 变异 > 120 ms 且呈周期性 → 窦性心律不齐
      否则 → 窦性心律

4. 交界性/室性来源者按心率细分:
      交界性: 40–60 逸搏心律 / 60–100 加速性 / >100 交界性心动过速
      室性:   20–40 逸搏心律 / 40–100 加速性室性自主心律 / >100 室性心动过速
```

#### 10.6.2 心房颤动的三重证据要求

**只用 RR 不规则判定房颤会在频发房性早搏时产生大量假阳性。** 必须三项同时满足：

| # | 条件 |
|---|---|
| 1 | `RR_IRREGULAR_ABSOLUTE`：`RR_CV > 0.15` 且 `RR_entropy > T_ent` 且 `SD1/SD2 > 0.4` |
| 2 | `P_ABSENT`：可辨 P 波占比 < 20%（在 II、V1 上评估） |
| 3 | f 波谱形：3–9 Hz 内无尖锐主峰（`f_wave_regularity < 3`）；TQ 段过短时该项置 `UNKNOWN` |

第 3 项为 `UNKNOWN` 时，若 1、2 成立仍可输出，但置信度降为 MEDIUM。

**与频发房性早搏的鉴别**：房早二联律/三联律的 Poincaré 图呈多簇聚集（`SD1/SD2` 低），房颤呈单一弥散云团。该特征是区分二者的最有效手段，必须实现。

#### 10.6.3 心房扑动

```
F_WAVE_PRESENT
AND 心房率 ∈ [240, 340] 次/分
AND 锯齿波在 II、III、aVF 或 V1 可辨
→ 心房扑动
   传导比 = 心房率 / 心室率，四舍五入至整数或标注"不等比传导"
```

**强制触发规则**：心室率在 140–160 之间的规整窄 QRS 心动过速，必须强制执行房扑检查支路（2:1 传导的房扑极易被误判为窦性心动过速）。即使 F 波未检出，也应在报告中提示"心室率接近 150，需除外 2:1 传导的心房扑动"。

#### 10.6.4 异位搏动

| 陈述 | 条件 |
|---|---|
| 室性早搏 | 心搏类型为 V（见 6.3），按数量输出：偶发（< 1%）/ 频发（≥ 1%）/ 成对 / 二联律 / 三联律 |
| 房性早搏 | 心搏类型为 S |
| 室性早搏多源 | 存在 ≥ 2 种形态不同的 V 类模板 |
| 非持续性室性心动过速 | 连续 V 类心搏 ≥ 3 且 < 30 s，率 > 100 |
| 逸搏 | 联律间期 > 1.15 × 中位 RR |

联律模式判定：二联律 = 每个主导型心搏后跟随一个异位搏，重复 ≥ 3 次。

#### 10.6.5 房室传导阻滞

判定前提：存在可辨的 P 波序列（`P_presence_ratio > 0.5`）。房颤时全部置 `UNKNOWN`。

| 分度 | 判定式 |
|---|---|
| 一度 | `PR_int > 200 ms` 且所有 P 波均下传 |
| 二度 I 型（文氏） | 存在 P 波未下传；脱落前 PR 进行性延长（总增幅 > 40 ms）；脱落后首个 PR 短于脱落前最后一个 PR ≥ 30 ms；RR 进行性缩短 |
| 二度 II 型 | 存在 P 波未下传；脱落前后 PR 恒定（SD < 10 ms） |
| 2:1 房室阻滞 | 恰好每 2 个 P 波下传 1 个，**无法区分 I 型与 II 型** |
| 高度房室阻滞 | ≥ 2 个连续 P 波未下传，但仍存在下传心搏 |
| 三度（完全性） | `AV_DISSOCIATION` 且心房率 > 心室率 且 PP 与 RR 各自规整 |

**2:1 阻滞必须单独输出为一个类别**，不得强行归入 I 型或 II 型。报告措辞："2:1 房室阻滞，本图无法区分莫氏 I 型与 II 型"。

**三度阻滞的必要条件是房室分离 + 心房率快于心室率**。仅有房室分离而心室率快于心房率的情况是干扰性房室分离（如加速性交界性心律），不是三度阻滞。这是常见误判点。

#### 10.6.6 宽 QRS 心动过速鉴别

进入条件：`QRS_dur ≥ 120 ms` 且 `HR > 100` 且持续 ≥ 3 个心搏。

按 Brugada 四步法顺序判定，任一步为"是"即判为室性心动过速：

| 步骤 | 判定 |
|---|---|
| 1 | V1–V6 全部导联均无 RS 形态？ |
| 2 | 任一胸导的 RS 间期（R 波起点 → S 波最低点）> 100 ms？ |
| 3 | 存在房室分离（`AV_DISSOCIATION`）、融合波或心室夺获？ |
| 4 | V1/V2 与 V6 的形态学符合室性标准？ |

四步均为"否" → 室上性心动过速伴室内差异性传导。

**支持室速的附加证据**（用于提升置信度）：胸导同向性（V1–V6 QRS 主波方向一致）、电轴落在 `AXIS_EXTREME`（无人区）。

**安全策略（强制）**：
```
IF 宽QRS心动过速:
    无论四步法结论如何，均设置 CRITICAL 标志与强制人工复核
    IF 四步法结论为 SVT:
        措辞 = "宽 QRS 心动过速，形态学倾向室上性伴差异传导，
                但不能排除室性心动过速，建议立即人工判读"
```

理由：将室速误判为室上速的临床后果远重于反向误判。规则系统在此处必须偏向保守。

### 10.7 M5 —— 复极异常、电解质与药物

所有本节结论均须附加"心电图表现提示，需实验室检查或临床信息确认"。

| 陈述 | 判定式 |
|---|---|
| QT 间期延长 | `QTcB > 450 ms`（男）/ `> 460 ms`（女） |
| QT 间期显著延长 | `QTcB > 500 ms` → 提升至急症优先级 |
| QT 间期缩短 | `QTcB < 340 ms` |
| Brugada 1 型（提示） | V1、V2 中 ≥ 1 个导联 `ST_J ≥ 200 μV` 且 ST 呈穹窿型下斜（`ST_slope < 0` 且 `ST_shape = CONVEX`）后接倒置 T 波 |
| 早期复极 | 下壁（II, III, aVF）或侧壁（I, aVL, V4–V6）≥ 2 个相邻导联存在 J 点切迹或顿挫，J 点抬高 ≥ 100 μV |
| 高钾血症（提示） | T 波高尖对称（`T_amp > 1.5 × 正常上限` 且 `T_symmetry ∈ [0.9,1.1]` 且 T 波基底窄）+ `P_amp[II] < 100 μV` 或 P 波消失 + `QRS_dur` 较预期增宽 |
| 低钾血症（提示） | `U_amp > 0.5 × T_amp` + ST 压低 + T 波低平（`\|T_amp\| < 100 μV`） |
| 低钙血症（提示） | `QTcB` 延长而 T 波形态与时限正常（ST 段延长所致） |
| 高钙血症（提示） | `QTcB` 缩短，ST 段几乎消失 |
| 洋地黄效应 | ST 呈"鱼钩"样下斜型压低（`ST_slope < 0` 且 `ST_shape = CONCAVE` 向上凹）+ QT 缩短 + T 波低平或倒置，多见于 V5、V6 |

**QT 测量的门控**：`QRS_dur ≥ 120 ms` 时（束支阻滞、起搏），QT 延长部分来源于除极延迟。此时应输出 `JT` 间期（`QT − QRS_dur`）作为辅助，并在报告中标注"QRS 增宽，QTc 判读受限"。

**Brugada 的重要限制**：标准 V1、V2 位置对 Brugada 型改变敏感度不足，需在第 2、3 肋间加做。系统检出可疑图形时应输出"建议加做高位胸导（V1、V2 上移 1–2 肋间）"。

### 10.8 M6 —— 其他

| 陈述 | 判定式 |
|---|---|
| 肢导低电压 | 所有 6 个肢导 `QRS_amp_pp < 500 μV` |
| 胸导低电压 | 所有 6 个胸导 `QRS_amp_pp < 1000 μV` |
| 全导联低电压 | 上述两者同时成立 |
| 电交替 | `QRS_alternans > 0.1` 且持续 ≥ 6 个心搏；与低电压并存时提升为"提示心包积液/心脏压塞" |
| 急性心包炎（提示） | ≥ 6 个导联广泛 ST 抬高且呈凹面向上（`ST_shape = CONCAVE`）+ PR 段压低（`PR_seg < −50 μV`）+ aVR 的 PR 段抬高 + **无镜像 ST 压低**（除 aVR、V1 外）+ 无病理性 Q 波 |
| 肺栓塞（提示，低敏感度） | `S_amp[I] > 150 μV` + `Q_amp[III] > 150 μV` + `T_amp[III] < 0`（S1Q3T3）+ 以下任一：不完全性/完全性 RBBB、V1–V4 T 波倒置、`AXIS_RIGHT` |
| 右位心 | 见 4.4.3 |
| 导联反接 | 见 4.4.1、4.4.2 |
| 非特异性 ST-T 改变 | 存在 ST 或 T 波异常但不满足任何上述特异性模式 |

**心包炎与 STEMI 的鉴别是本模块最关键也最容易出错的一项。** 关键区分点是"广泛分布 + 无镜像压低 + PR 段改变"。判定不确定时，**优先输出 STEMI 并强制人工复核**（漏诊 STEMI 的后果远重于误报心包炎）。

---

## 11. L8 冲突消解、优先级与序列比对

### 11.1 互斥规则表

| 组 | 互斥成员 | 消解策略 |
|---|---|---|
| 束支阻滞 | LBBB / RBBB / IVCD | 三者互斥。同时成立时按 LBBB > RBBB > IVCD 优先 |
| 分支阻滞 | LBBB 与 LAFB/LPFB | LBBB 成立时抑制分支阻滞输出 |
| 预激 | 预激与任何束支阻滞 | 预激优先，抑制束支阻滞输出 |
| 基础节律 | 窦性 / 房颤 / 房扑 / 交界性 / 室性 | 互斥，单选。冲突时按证据强度（满足条件数）排序取最高 |
| 房室阻滞 | 各分度 | 互斥，取最高度数 |
| 正常判定 | "正常心电图" | 与任何异常陈述互斥 |
| 肥大 | "左心室肥大" 与 "左心室高电压" | 互斥，前者优先 |

### 11.2 输出优先级（报告排序）

| 级别 | 内容 |
|---|---|
| P0 技术警告 | 导联反接、信号质量不足、右位心 |
| P1 急症 | ST 抬高型心肌梗死、宽 QRS 心动过速、三度/高度房室阻滞、心室率 < 40 或 > 150、`QTcB > 500`、Brugada 1 型、高钾提示、左主干型改变 |
| P2 节律 | 基础节律、异位搏动、房室阻滞（一度、二度） |
| P3 传导 | 束支阻滞、分支阻滞、预激、IVCD |
| P4 结构 | 心房异常、心室肥大 |
| P5 陈旧改变 | 陈旧性心肌梗死、演变期改变 |
| P6 非特异 | 非特异性 ST-T 改变、低电压等 |

同级内按置信度降序排列。

### 11.3 序列比对（若存在既往心电图）

序列比对是规则系统能提供的最高临床价值功能之一，且实现成本低于新增诊断模块。**强烈建议纳入 v1.0 范围。**

比对项与显著变化阈值（超出复测生理变异范围）：

| 测量项 | 显著变化阈值 |
|---|---|
| `QRS_dur` | ±10 ms |
| `QT_int` | ±25 ms |
| `PR_int` | ±20 ms |
| `ST_J`（任一导联） | ±100 μV |
| `QRS_axis` | ±30° |
| `T_amp`（任一导联） | 极性反转，或 ±200 μV |

触发规则：

```
IF 新出现 病理性Q波:                → 输出"较前新出现 Q 波"，提升至 P1
IF 新出现 ST 抬高 ≥ 100 μV:        → 提升至 P1
IF T 波极性由正转负（≥2 相邻导联）: → 输出"较前 T 波演变"，提升至 P2
IF 基础节律改变（如窦性 → 房颤）:   → 输出"较前节律改变"，提升至 P1
IF 无显著变化:                      → 输出"与 [日期] 心电图比较大致相同"
```

比对前必须校验两份记录的采集条件（导联位置、滤波设置）一致，不一致时标注"采集条件不同，比对结果仅供参考"。

---

## 12. L9 报告输出与弃权

### 12.1 报告结构

```
【技术信息】
  采集时间、设备、滤波设置、信号质量评级、可用导联

【测量值表】
  HR、P 时限、PR、QRS、QT、QTcB、QTcFri、P/QRS/T 电轴

【诊断陈述】（按 11.2 优先级排序）
  每条包含：陈述文本 + 置信度限定词 + 证据链引用

【弃权项】
  因门控或数据缺失而无法评估的项目，逐条列出原因

【复核标志】
  是否需要人工复核（及原因）

【声明】
  本结果由计算机自动生成，为辅助判读参考，不构成诊断结论，
  须由具备资质的医师复核。
```

### 12.2 置信度到措辞的映射（必须固定并全系统一致）

| 置信度 | 措辞模板 | 含义 |
|---|---|---|
| HIGH | "〈诊断〉" | 全部必要判据满足，测量可靠 |
| MEDIUM | "考虑〈诊断〉" | 必要判据满足但存在测量降级因素，或部分支持性判据未满足 |
| LOW | "可能〈诊断〉，建议结合临床" | 判据部分满足，或所用判据本身特异度有限 |
| 弃权 | "因〈原因〉，无法评估〈项目〉" | 门控关闭或输入为 UNKNOWN |
| 提示 | "提示〈发现〉，建议人工复核" | 高危形态学模板命中 |

AHA/ACCF/HRS 指出"probable""possible""consider"这类限定词容易被多种解读，可用于表示部分满足标准或接近阈值。因此**本系统必须在本表中固定每个限定词的精确含义，并禁止在任何模块中偏离该定义**。

### 12.3 "正常心电图"的输出条件

必须**全部**满足才可输出"正常心电图"：

```
1. L2 质量门控：12 导全部可用，无反接警告
2. 全部异常 finding 均为 FALSE（无一为 TRUE）
3. 全部 finding 均非 UNKNOWN
4. 全部测量值落在正常范围内：
     HR ∈ [60, 100]
     P_dur < 120 ms
     PR_int ∈ [120, 200] ms
     QRS_dur < 110 ms
     QTcB ≤ 450 (男) / 450 (女) ms   ← 采用更严格的下限
     QRS_axis ∈ [−30°, +90°]
5. 无 MORPH_COMPLEX、MEDIAN_BEAT_WEAK 标志
```

**存在任何 UNKNOWN 时禁止输出"正常"**，改为："未见明确异常，但〈项目列表〉无法评估。"

这一条是安全性的关键。系统绝不能因为"没有检出异常"就宣告正常——没有检出可能是因为无法评估。

### 12.4 强制人工复核触发条件

以下任一成立时，报告须置顶显示复核标志：

- 任何 P0 或 P1 级陈述
- 任何"提示"级陈述（高危形态学模板命中）
- 存在 ≥ 3 个弃权项
- `MORPH_COMPLEX` 或 `MEDIAN_BEAT_WEAK`
- 宽 QRS 心动过速（无论结论）
- 疑似心包炎与 STEMI 无法区分
- 可用导联 < 12

### 12.5 陈述编码

所有输出陈述须映射到标准化编码，建议采用 SCP-ECG 陈述码或 MDC_ECG 编码体系，并预留到 SNOMED CT 的映射字段。

理由：不同厂商算法的陈述文本与数据结构互不兼容，若不做标准化，后续的数据分析、质控统计与跨系统集成将无法进行。这一层在 v1.0 就要建好，后补成本极高。

---

## 13. 参数配置总表

**所有阈值必须集中于单一配置文件，禁止硬编码在算法代码中。** 配置文件应带版本号，且报告中记录所用配置版本。

### 13.1 信号处理参数

| 参数 | 默认值 | 可调范围 | 说明 |
|---|---|---|---|
| `FS_MIN` | 500 Hz | — | 低于此值拒绝处理 |
| `HP_CUTOFF` | 0.05 Hz | 0.05–0.67 | 线性相位滤波器可用上界 |
| `LP_CUTOFF_ADULT` | 150 Hz | 100–150 | |
| `LP_CUTOFF_PED` | 250 Hz | 150–250 | 需 fs ≥ 1000 Hz |
| `MEDFILT_WIN1` | 200 ms | — | 基线校正 |
| `MEDFILT_WIN2` | 600 ms | — | 基线校正 |
| `NOTCH_Q` | 30 | 20–30 | 上限受振铃约束 |

### 13.2 质量门控参数

| 参数 | 默认值 |
|---|---|
| `T_FLATLINE_AMP` | 50 μV |
| `T_BASELINE_DRIFT` | 0.30 |
| `T_POWERLINE` | 0.15 |
| `T_EMG` | 0.40 |
| `T_BSQI` | 0.85 |
| `MIN_USABLE_LEADS` | 6 |

### 13.3 定界参数

| 参数 | 默认值 |
|---|---|
| `QRS_THRESH_K` | 0.35 |
| `REFRACTORY` | 200 ms |
| `MISSED_BEAT_FACTOR` | 1.5 |
| `XI_ON` | 0.05 |
| `XI_OFF` | 0.10 |
| `T_SEARCH_MAX` | 600 ms |
| `MIN_SUPPORT_LEADS` | 4 |

### 13.4 心搏聚类参数

| 参数 | 默认值 |
|---|---|
| `TEMPLATE_CORR_THRESH` | 0.90 |
| `TEMPLATE_MERGE_THRESH` | 0.95 |
| `VENTRICULAR_CORR_MAX` | 0.80 |
| `PREMATURITY_RATIO` | 0.85 |
| `DOMINANT_RATIO_MIN` | 0.30 |

### 13.5 诊断阈值

全部诊断阈值见第 10 章各表，实现时统一抽取到配置文件的 `diagnostic_thresholds` 节。每条阈值须附文献来源字段，便于审计与后续调整。

---

## 14. 验证与测试计划

### 14.1 分层验证原则

**每一层独立验证，不得只测端到端。** 端到端指标好看但无法定位问题，且规则系统的错误往往是某一层的系统性偏差被下游放大。

### 14.2 各层验收标准

| 层 | 数据集 | 指标 | 目标 |
|---|---|---|---|
| L2 反接检测 | 人工构造的反接样本（从正常记录按 4.4.1 的变换关系合成）+ 真实反接样本 | Se / Sp | 肢导反接 Se ≥ 95%，Sp ≥ 99% |
| L3 QRS 检测 | MIT-BIH Arrhythmia、NST（噪声压力测试） | Se / +P，依 ANSI/AAMI EC57 协议 | ≥ 99.5% / ≥ 99.5% |
| L3 波形定界 | QT Database、LUDB | 起终点误差的均值与标准差 | P 起终点 SD ≤ 10 ms；QRS SD ≤ 8 ms；T 终点 SD ≤ 25 ms |
| L5 测量精度 | CSE Multilead 数据库 | 与参考值的平均差 | P 时限、PR、QRS 各 ±10 ms 内；QT、RR 各 ±25 ms 内。标准差限值须按 CSE 标准原文核对 |
| L7 诊断 | 见 14.3 | 逐类 Se / Sp / PPV / NPV | 见 14.4 |

CSE 的平均差限值是本系统测量层能否被认为达到诊断级的分水岭。已发表的商用与研究级算法均以此作为通过标准。

### 14.3 诊断层测试集构成

| 用途 | 数据集 | 规模 | 说明 |
|---|---|---|---|
| 开发调参 | PTB-XL | 约 21,800 条 10 秒 12 导记录、18,869 例患者、500 Hz、71 类陈述 | 使用官方按患者划分的 10 折，前 8 折调参 |
| 内部验证 | PTB-XL 第 9、10 折 | — | 调参期间不得接触 |
| 外部验证 A | CPSC2018 | 6,877 条、9 类标签、500 Hz、6–60 秒 | 中国人群，与国内落地场景更接近 |
| 外部验证 B | Chapman-Shaoxing / Ningbo | 约 45,000 条、10 秒、500 Hz | GE MUSE 采集，设备差异测试 |
| 外部验证 C | PhysioNet/CinC 2020–2021 合集（含 Georgia） | — | 多设备、多地域 |
| 罕见类补充 | 自建专项集 | 每类 ≥ 30 例 | 三度房室阻滞、Brugada、de Winter、Wellens、右位心等公开集中样本极少的类别 |
| 金标准验收集 | **自建** | ≥ 2,000 例 | 见 14.5 |

### 14.4 指标要求

**禁止使用 accuracy 作为主指标**（类别极不平衡时无意义）。

| 指标 | 用途 |
|---|---|
| 逐类敏感度（Se） | 主指标。安全关键类（STEMI、VT、三度 AVB）须单独设定并达标 |
| 逐类特异度（Sp）与 PPV | 控制误报导致的复核负担 |
| 弃权率 | 单独统计。**弃权不计入错误**，但弃权率过高（> 20%）说明门控过严，需调参 |
| macro-F1 | 整体参考 |

安全关键类的建议目标（需按临床场景协商确定）：

| 类别 | Se 目标 | Sp 目标 |
|---|---|---|
| ST 抬高型心肌梗死 | ≥ 90% | ≥ 95% |
| 室性心动过速 | ≥ 95% | ≥ 90% |
| 三度房室阻滞 | ≥ 95% | ≥ 98% |
| 心房颤动 | ≥ 95% | ≥ 97% |
| 完全性左/右束支阻滞 | ≥ 95% | ≥ 98% |

### 14.5 关于公开数据集标签的重要警告

**公开数据集的标签相当一部分来源于设备自带算法的输出 + 人工复核。** 直接以其为金标准调参，等于在拟合其他厂商算法的错误，且会掩盖真实性能。

必需的应对措施：建立一个 ≥ 2,000 例的自建金标准集，由 ≥ 2 名心电图专业医师独立判读、分歧由第三人仲裁，作为最终验收依据。开发过程中该集合严格封存。

### 14.6 回归测试

规则系统的最大风险是**修补一个边界样本时破坏另一个已通过的样本**。必须建立：

- 每个诊断规则至少 5 个正例 + 5 个负例的单元测试样本
- 三值逻辑求值器的完整真值表测试
- 每次规则变更后跑全量回归，任何已通过样本转为失败即阻断合入
- 门控矩阵（9.4）的组合覆盖测试

### 14.7 鲁棒性测试

| 测试 | 方法 | 通过条件 |
|---|---|---|
| 噪声耐受 | 在干净记录上按 NST 协议叠加基线漂移、肌电、工频噪声，逐级提高强度 | 性能随噪声增大应表现为**弃权率上升**，而非误判率上升 |
| 滤波域偏移 | 用不同前置滤波设置重新处理同一原始记录 | 诊断结论一致性 ≥ 95% |
| 导联缺失 | 逐一屏蔽单个导联 | 依赖该导联的诊断应正确弃权，不依赖的应保持结论 |
| 采样率 | 500 / 1000 Hz 同源数据对比 | 测量差异在验收限内 |

第一项是本系统设计哲学的直接检验：**噪声下宁可弃权，不可猜测。**

---

## 15. 风险与已知局限

### 15.1 技术路线固有局限（须书面告知使用方）

| 局限 | 表现 | 缓解措施 |
|---|---|---|
| 非典型形态学敏感度低 | de Winter、Aslanger 型、非典型 Wellens 等的规则化实现召回有限 | 输出为"提示"级并强制复核；报告中声明不覆盖全部急性冠脉闭塞表现 |
| 阈值脆性 | 测量值恰在阈值附近时结论不稳定 | 置信度降级机制（8.1）；边界样本输出限定词而非确定陈述 |
| 噪声鲁棒性弱 | 中重度噪声下定界失败 | 质量门控 + 高弃权率 |
| 无法学习本地人群特征 | 阈值来自国际文献，与本地人群分布可能有偏差 | 上线后统计各判据的阳性率，与流行病学预期比对，必要时人工调整配置（**不是**自动拟合） |

### 15.2 数据与场景局限

| 局限 | 说明 |
|---|---|
| 10 秒记录的根本限制 | 阵发性心律失常、间歇性阻滞可能完全不出现。系统必须严格区分"本图未见"与"排除"，报告措辞不得使用"排除"字样 |
| 缺乏临床信息 | 电解质、药物、体型、症状均不可得，多条判据的排除条件无法完整执行 |
| 无右胸与后壁导联 | 右室与后壁梗死只能间接推断，须主动建议加做 |
| 儿科适用性 | 本规格的阈值基于成人。**儿科需要独立的年龄分层阈值体系，不在 v1.0 范围内**，须在系统中显式拒绝处理 < 16 岁的记录或标注"阈值不适用" |

### 15.3 需要项目层面决策的开放问题

1. 年龄/性别缺失时采用策略 A（弃权）还是策略 B（保守阈值）
2. 弃权率的可接受上限（影响临床工作流的复核负担）
3. 是否纳入序列比对（建议纳入，但需要既往数据接入能力）
4. 安全关键类的 Se/Sp 目标（需与临床方协商，二者不可兼得）
5. 陈述编码体系的选择（SCP-ECG vs MDC_ECG）

### 15.4 监管相关

若拟作为医疗器械软件提交注册：

- 相关标准包括 IEC 60601-2-25（诊断心电图机）与 ISO/IEC 80601-2-86。后者的重要变化是：**算法测试要求、测试方法与数据集将依据算法的预期用途适用，而非仅依据承载算法的设备类型**。因此"我们只是软件"不构成豁免理由
- 纯规则路线在递交上的优势是显著的：无需提交训练数据代表性、无需数据漂移监控方案、无需再训练变更控制流程。**这是选择本路线的主要收益，应在立项文档中明确记录**
- 配置文件的每次阈值变更都属于软件变更，须走变更控制流程并保留文献依据

---

## 16. 实施路线图

### 16.1 阶段划分

| 阶段 | 内容 | 交付物 | 建议周期 |
|---|---|---|---|
| **P1 基础设施** | L0 数据接入、L1 滤波、配置管理框架、三值逻辑求值器、回归测试框架 | 可运行的数据管道 | 3–4 周 |
| **P2 安全屏障** | L2 全部：SQI、导联可用性、反接检测、右位心鉴别 | 质量门控模块 + 反接检测验收报告 | 3–4 周 |
| **P3 测量核心** | L3 定界 + L4 聚类与中位搏 | 通过 QT Database / LUDB / CSE 验收 | 8–10 周 |
| **P4 特征与发现层** | L5 全部特征、L6 全部 finding | 特征字典实现 + 单元测试全覆盖 | 4–5 周 |
| **P5 推理骨架** | 门控 DAG、L8 冲突消解、L9 报告框架、弃权机制 | 可输出报告的空规则系统 | 3 周 |
| **P6 规则库 A** | M0 起搏、M2 传导、M1 节律、房室阻滞 | 通过对应类别验收 | 6–8 周 |
| **P7 规则库 B** | M3 肥大、M5 复极、M6 其他 | 同上 | 4–5 周 |
| **P8 规则库 C** | M4 缺血梗死（含高危模板、Sgarbossa） | 同上 | 6–8 周 |
| **P9 序列比对** | L8 序列比对模块 | | 3 周 |
| **P10 系统验收** | 金标准集验收、鲁棒性测试、文档 | 验收报告 | 4 周 |

### 16.2 排序理由

- **P2 早于 P3**：质量门控是规则系统的生命线，必须先于测量建立。先做定界再补门控会导致大量返工
- **P3 是长周期高风险项**：定界精度决定系统上限，不要压缩此阶段。若 CSE 验收不通过，后面全部工作的价值都要打折
- **P4 与 P5 之间不可跳跃**：发现层与规则层的解耦是可维护性的关键，不要为赶进度直接从特征写到诊断
- **P6 先于 P8**：节律与传导的规则确定性最高、临床需求最刚性、验收最容易通过，先做能建立信心并暴露架构问题；缺血梗死最难，放在架构稳定后做
- **P9 序列比对成本低、临床价值高**，若资源紧张可与 P7 并行

### 16.3 里程碑门禁

| 门禁 | 条件 |
|---|---|
| G1（P3 出口） | CSE 测量精度全部达标，QRS 检测达 EC57 目标 |
| G2（P6 出口） | 节律与传导类在外部验证集上 Se ≥ 90% |
| G3（P8 出口） | 安全关键类达 14.4 目标，且噪声测试表现为弃权率上升 |
| G4（P10 出口） | 自建金标准集验收通过，全部文档齐备 |

任一门禁不通过时不得进入下一阶段。

---

## 附录 A —— 完整 finding 清单（实现检查表）

按模块分组，实现时逐项打勾。带 ★ 者为安全关键项，须有专项测试。

**质量类**：`SIGNAL_QUALITY_POOR` ★、`LEAD_UNUSABLE[l]`、`LEAD_SWAP_LA_RA` ★、`LEAD_SWAP_RA_LL` ★、`LEAD_SWAP_LA_LL` ★、`LEAD_SWAP_RA_RL`、`LEAD_SWAP_LL_RL`、`PRECORDIAL_SWAP` ★、`DEXTROCARDIA` ★

**时间类**：`HR_BRADY`、`HR_TACHY`、`PR_PROLONGED`、`PR_SHORT`、`PR_PROGRESSIVE`、`PR_CONSTANT`、`QRS_WIDE`、`QRS_BORDERLINE`、`QT_PROLONGED`、`QT_SEVERE` ★、`QT_SHORT`、`P_DUR_PROLONGED`

**电轴类**：`AXIS_NORMAL`、`AXIS_LEFT`、`AXIS_RIGHT`、`AXIS_EXTREME`、`POOR_R_PROGRESSION`、`EARLY_TRANSITION`

**形态类**：`V1_QS_OR_rS`、`LATERAL_NOTCHED_R`、`NO_Q_LATERAL`、`VAT_LATERAL_LONG`、`V1_RSR_PATTERN`、`WIDE_S_LATERAL`、`DELTA_WAVE`、`PATHOLOGIC_Q[region]` ★、`QRS_NOTCH[l]`、`TERMINAL_QRS_DISTORTION` ★

**起搏类**：`PACING_SPIKE_PRESENT`、`PACED_VENTRICULAR`、`PACED_ATRIAL`、`PACED_DUAL`、`PACING_FAILURE_CAPTURE`、`PACING_UNDERSENSING`

**节律类**：`RR_IRREGULAR_ABSOLUTE`、`RR_REGULAR`、`RR_REGULARLY_IRREGULAR`、`P_ABSENT`、`P_UNIFORM`、`P_MULTIFORM`、`P_RETROGRADE`、`F_WAVE_PRESENT`、`AV_DISSOCIATION` ★、`P_NONCONDUCTED` ★

**ST-T 类**：`ST_ELEVATION[region]` ★、`ST_DEPRESSION[region]`、`T_INVERSION[region]`、`ST_STRAIN_PATTERN`、`DE_WINTER_PATTERN` ★、`WELLENS_PATTERN` ★、`LEFT_MAIN_PATTERN` ★、`POSTERIOR_MI_PATTERN` ★、`SGARBOSSA_POSITIVE` ★、`BRUGADA_TYPE1` ★、`EARLY_REPOL`、`PERICARDITIS_PATTERN` ★

**电压类**：`LVH_SOKOLOW`、`LVH_CORNELL_V`、`LVH_CORNELL_P`、`LVH_PEGUERO`、`LVH_ROMHILT[score]`、`RVH_CRITERIA[n]`、`LOW_VOLTAGE_LIMB`、`LOW_VOLTAGE_PRECORDIAL`、`ELECTRICAL_ALTERNANS`

**心房类**：`LEFT_ATRIAL_ABNORMALITY`、`RIGHT_ATRIAL_ABNORMALITY`

**电解质提示类**：`HYPERKALEMIA_PATTERN` ★、`HYPOKALEMIA_PATTERN`、`HYPOCALCEMIA_PATTERN`、`HYPERCALCEMIA_PATTERN`、`DIGITALIS_EFFECT`

---

## 附录 B —— 缩略语

| 缩写 | 全称 |
|---|---|
| AVB | 房室传导阻滞 |
| BBB | 束支阻滞 |
| IVCD | 非特异性室内传导延迟 |
| LAFB / LPFB | 左前分支阻滞 / 左后分支阻滞 |
| LVH / RVH | 左心室肥大 / 右心室肥大 |
| PTFV1 | V1 导 P 波终末电势 |
| SQI | 信号质量指数 |
| STEMI | ST 抬高型心肌梗死 |
| SVT | 室上性心动过速 |
| VAT | 心室激动时间（R 峰时间、本位曲折） |
| VT | 室性心动过速 |
| WPW | 预激综合征（Wolff-Parkinson-White） |

---

## 附录 C —— 关键参考来源

- AHA/ACCF/HRS 心电图标准化与判读推荐系列（Part I 技术、Part III 室内传导异常、Part V 心腔肥大）
- 第四版心肌梗死通用定义（ST 段抬高与病理性 Q 波阈值）
- ANSI/AAMI EC57（心搏检测算法测试协议）
- IEC 60601-2-25、ISO/IEC 80601-2-86（诊断心电图设备与算法测试要求）
- CSE（Common Standards of Electrocardiography）多导联数据库与测量精度限值
- PhysioNet：MIT-BIH Arrhythmia、NST、QT Database、LUDB、PTB-XL、CPSC2018、Chapman-Shaoxing

---

**文档结束**