# 多导联心电 T 波定界与 ST 段特征提取技术规范

**方法范围：纯确定性信号处理（小波、微分、面积、几何、曲线拟合、稳健统计）。不含任何机器学习／深度学习／HMM 方法。**

---

## 0. 约定与符号

| 项 | 约定 |
|---|---|
| 采样率 | 全文以 `fs = 500 Hz`（2 ms/样点）为基准，括号内给出 1000 Hz 换算 |
| 幅度单位 | µV |
| 时间基准 | 全局 R 峰位置 `R_k`（多导联融合后的单一时刻） |
| 基准点 | `QRSon`（QRS 起点）、`J`（QRS 终点/J 点）、`Ton`、`Tpk`、`Toff`、`B`（等电位基线电平） |
| 导联集 | 12 导；独立导联 8 个：I, II, V1–V6（其余为线性组合） |

> **阈值说明**：下文所有阈值分三类标注 ——
> `【文献】` 来自公开算法/标准的常用取值；
> `【工程】` 工程实践经验值，需在自己的数据上标定；
> `【临床】` 临床判读标准，仅作为特征取值参考，**不构成诊断输出**。

---

## 1. 前处理

前处理的系统误差通常大于算法差异本身。T 终点的总误差预算约 ±30 ms，其中前处理若不当可单独吃掉 15 ms 以上。

### 1.1 采样率

| 用途 | 最低 | 推荐 |
|---|---|---|
| 常规 T 定界 | 500 Hz | 500 Hz |
| QT 精密测量 / TpTe | 500 Hz | 1000 Hz |
| ST 电平 | 250 Hz | 500 Hz |

原始 ≤250 Hz 的信号建议先用三次样条或多相 FIR 插值到 1000 Hz。这不增加信息量，但消除样点栅格带来的 ±2 ms 量化误差；对 TpTe（典型 80–100 ms）这类小量的相对误差影响显著。

### 1.2 基线漂移去除（对 ST 影响最大的一步）

**推荐：级联中值滤波**（PhysioNet 惯例）

```
y1 = medfilt(x,  W1)     # W1 = 200 ms → 101 样点 @500Hz（201 @1000Hz）
y2 = medfilt(y1, W2)     # W2 = 600 ms → 301 样点 @500Hz（601 @1000Hz）
out = x - y2
```

窗长必须为奇数样点。W1 需大于最宽 QRS（覆盖 ~120 ms），W2 需大于最宽 QRS+ST+T。

**备选 A：等电位点三次样条**
- 节点位置：每拍 `QRSon − 30 ms`（PQ 段中点），取该点 ±10 ms 窗的中位数作为节点纵坐标
- 心率 >100 bpm 时 PR 段太短，改用 `QRSon − 20 ms` 处 ±6 ms 窗
- 节点缺失（P 波不可辨、房颤）时退化到中值滤波

**备选 B：零相位高通**
- Butterworth 4 阶，`fc = 0.05 Hz`，`filtfilt` 前后向滤波

**❌ 禁止**：`fc = 0.5 Hz` 的单向 IIR 高通。实测可在 ST 段引入 50–100 µV 的伪抬高/压低，方向与 QRS 极性相关，是 ST 特征最隐蔽的系统误差源。

### 1.3 工频干扰

- 优先：自适应陷波，中心频率 50/60 Hz，`Q ≈ 35`（−3 dB 带宽 ≈ 1.4 Hz），零相位实现
- 若仅做 T/ST 分析：直接低通 40 Hz 已完全覆盖，无需单独陷波

### 1.4 分支滤波（关键设计）

**不同任务用不同滤波分支，禁止全流程共用一条滤波链。**

| 分支 | 滤波 | 用途 |
|---|---|---|
| `x_qrs` | 带通 5–15 Hz（Pan-Tompkins）或 8–20 Hz | QRS 检测、拍对齐 |
| `x_fid` | 零相位低通 40 Hz，FIR 等波纹，阶数 ≈ 0.1·fs | QRSon / J 点定位（需保留 QRS 末端高频） |
| `x_t` | 零相位低通 **20–25 Hz**，FIR | T 波定界与形态（T 能量 <10 Hz） |
| `x_amp` | 零相位低通 40 Hz | ST 电平、T 幅度等幅度类测量 |

`x_t` 若使用 15 Hz 低通，抗噪进一步提升，但会加剧 T–U 融合，需配合 §4.7 的 U 波判别。

### 1.5 伪迹与无效段标记

| 现象 | 判据 | 处置 |
|---|---|---|
| 起搏脉冲 | 宽度 <2 ms 且幅度 >2 mV 的窄尖峰 | 检出后线性插值填补 |
| ADC 饱和 | 连续 ≥8 样点处于满量程 ±1 LSB | 该段标记无效 |
| 电极脱落 | 50 ms 滑窗内峰峰值 <10 µV | 整导联剔除 |
| 突跳伪迹 | 单样点跳变 >1 mV 且相邻样点不延续 | 中值替换 |

### 1.6 中位拍（representative beat）构建

**T 定界应在中位拍上进行**，逐拍定界仅在需要 beat-to-beat 变异（TWA、QT 变异性）时使用，且必须以中位拍结果作为搜索先验。

```
1) QRS 对齐：在 x_qrs 上以模板互相关，搜索范围 ±30 ms，
   峰值处做三点抛物线插值 → 亚样点对齐精度
2) 形态聚类：
   - 相关系数 ρ ≥ 0.90 且 QRS 宽度差 ≤ 25 ms  → 归入同簇   【工程】
   - ρ < 0.85 或宽度差 > 25 ms                 → 新建簇（PVC/差传）
3) 主簇选取：拍数最多且平均 RR 变异最小的簇
4) 逐样点取【中位数】（不是均值，抗离群拍）
5) 输出窗：R − 250 ms  到  R + min(500 ms, 0.70·RR_median)
6) 置信门限：簇内拍数 ≥ 8 才输出高置信中位拍；3–7 拍降级；<3 拍不输出
```

SNR 提升约 √N。8 拍中位可将 T 终点的随机误差从 ±20 ms 降到 ±7 ms 量级。

---

## 2. 基准点与等电位基线

### 2.1 QRSon 与 J 点

**小波法（推荐）**：在二次样条小波尺度 2¹、2² 上定位。QRS 在这两个尺度上能量最集中。

- `QRSon`：R 峰前第一个模极大值之前，`|w₂|` 首次降到 `0.05 × max|w₂|` 处 【文献】
- `J`：R/S 后最后一个模极大值之后，`|w₂|` 降到 `0.125 × 该模极大值` 处 【文献】

**斜率精修（J 点）**：从 R 峰向后搜索，取 `|dy/dt|` 首次**连续 8 ms** 低于 QRS 段最大斜率 5% 的时刻 【工程】。

**不可靠标记**：QRS 宽度 >120 ms、存在 delta 波、或束支阻滞形态时，J 点定位误差可达 20 ms 以上，应置低置信标记，且 ST 测量点应统一后移。

### 2.2 等电位基线 `B`

| 优先级 | 窗口 | 附加条件 |
|---|---|---|
| 1 | PR 段：`[QRSon − 40 ms, QRSon − 10 ms]` | 窗内峰峰值 < 30 µV 【工程】 |
| 2 | PR 段内最平坦子窗：在上述窗内滑动 20 ms 子窗，取方差最小者 | 心率 <100 bpm |
| 3 | 短 PR：`[QRSon − 26 ms, QRSon − 6 ms]` | RR < 600 ms |
| 4 | TP 段：`[Toff + 40 ms, Pon − 20 ms]` | 需已知 Toff（第二遍迭代） |

`B` 取窗内**中位数**而非均值。

**逐导联独立计算 `B`**，不可跨导联共用。

---

## 3. T 波搜索窗

搜索窗定义直接决定漏检率与误检率。

```
Twin_start = J + max(60 ms, 0.04·RR)
Twin_end   = J + min(0.62·RR, 500 ms)          【工程】
```

按心率的实际取值（RR 为前一拍 RR）：

| HR (bpm) | RR (ms) | Twin_start (J+) | Twin_end (J+) | 备注 |
|---|---|---|---|---|
| 50 | 1200 | 60 ms | 500 ms | 上限封顶生效 |
| 60 | 1000 | 60 ms | 500 ms | 上限封顶生效 |
| 75 | 800 | 60 ms | 496 ms | |
| 100 | 600 | 60 ms | 372 ms | |
| 120 | 500 | 60 ms | 310 ms | 需防 P-on-T |
| 150 | 400 | 60 ms | 248 ms | T 与 P 高度重叠，置信降级 |

**RR 平滑**：用前 5 拍 RR 的中位数，避免单个早搏使窗口塌缩。

**P-on-T 防护**：当 `Twin_end` 距下一拍 `QRSon` 小于 120 ms 时，将 `Twin_end` 截断到 `nextQRSon − 120 ms`，并标记「T–P 重叠」。

---

## 4. 单导联 T 定界方法

### 4.1 小波模极大值法（主力方法）

**小波选择**：Mallat 二次样条小波（一阶导数型），滤波器 `h = [1,3,3,1]/8`、`g = [2,−2]`，采用 à trous 算法（无抽取，保持时间对齐）。

**尺度选择**：

| fs | T 波使用尺度 | 等效通带 |
|---|---|---|
| 250 Hz | 2³ | ~3–11 Hz |
| 500 Hz | 2⁴ | ~3–10 Hz |
| 1000 Hz | 2⁵ | ~3–10 Hz |

**步骤**：

```
1) 在搜索窗内计算 w4，噪声门限
     ε_T = max( 0.25 · RMS(w4 | Twin) ,  0.125 · max|w4 | Twin| )   【文献】

2) 提取所有 |w4| > ε_T 的模极大值，按时间排序

3) 形态判定与 T 峰定位：
   - 2 个异号模极大值        → 单相 T，两者之间的过零点 = Tpk
                               （模极大值对的符号顺序决定 T 的极性）
   - 3 个交替符号模极大值     → 双相 T，2 个过零点 = 两个峰
   - 4 个及以上              → 切迹/多相 T，取幅度最大的两个相邻异号极大值定主峰
   - 0 或 1 个               → T 波不可辨，返回 NaN 并标记

4) 边界定位：
   Ton  = 第一个模极大值【之前】，|w4| 首次降到 ξ_on · |第一模极大值| 的时刻
   Toff = 最后一个模极大值【之后】，|w4| 首次降到 ξ_off · |末模极大值| 的时刻

   参考阈值                                          【文献】
     ξ_on  = 0.25
     ξ_off = 0.40
   低幅 T（|Tpk − B| < 200 µV）建议 ξ_off = 0.50     【工程】

5) 兜底：若在最大搜索距离内未降到阈值，取 |w4| 的第一个局部最小点
     最大搜索距离：Toff 距末模极大值 ≤ 160 ms，Ton 距首模极大值 ≤ 120 ms  【工程】
```

**性能参考**：该方法在 CSE 库上 T 终点误差约 **0.8 ± 18 ms**（CSE 允许 2SD 容差为 30.6 ms）【文献】。

**优点**：对正向、倒置、双相 T 一视同仁；多尺度天然抗噪；不依赖等电线。
**弱点**：极低幅 T（<80 µV）时模极大值被噪声门限吞掉；T–U 融合时末模极大值可能落在 U 波上。

### 4.2 梯形面积法（低幅 T 的主力）

Zhang 等提出的 trapezium area 方法，**不依赖等电线**，因此对残余基线漂移免疫。

```
输入：x_t（低通 20–25 Hz），T 峰位置 Tpk

1) 在 [Tpk, Tpk + 200 ms] 内求一阶导数绝对值最大点 → (x_m, y_m)
   （即降支最大斜率点；一阶导数用 Savitzky-Golay，2 阶多项式、9 点窗）

2) 设参考点横坐标  x_r = x_m + 160 ms        【工程，范围 120–200 ms】

3) 对每个候选 x_i ∈ [x_m + 10 ms, x_r − 10 ms]，计算梯形面积：
       A(x_i) = 0.5 · |y_m − y(x_i)| · (2·x_r − x_i − x_m)

4) Toff = argmax_i A(x_i)
```

面积函数 `A` 在真实 T 终点处取极大：终点之前 `|y_m − y_i|` 仍在增长，终点之后 `(2x_r − x_i − x_m)` 快速衰减。

**参数敏感性**：`x_r` 取值影响结果约 ±5 ms。建议固定为 160 ms 并在自有数据上标定一次。

**优点**：低幅 T 下明显优于切线法与阈值法；抗基线漂移；实现简单、无迭代。
**弱点**：需要正确的 `Tpk` 先验；双相 T 需先确定用哪一相的降支。

### 4.3 切线法（临床对齐用）

与人工判读习惯最一致，做临床对标时必须实现。

```
1) 降支最大斜率点 (x_s, y_s)：一阶导数极值
   （Savitzky-Golay 2 阶 / 9 点，或 5 点中心差分 + 3 点平滑）
2) 斜率 k = dy/dt |_{x_s}
3) Toff = x_s + (B − y_s) / k
```

**使用前置条件（必须检查）** 【工程】：
- `|Tpk − B| ≥ 100 µV`，否则误差可 >40 ms，禁用
- 求得的 `Toff ≤ x_s + 200 ms`，否则判为失败
- `|k| ≥ 0.8 µV/ms`，斜率过缓时外推不稳定

**系统偏倚提示**：切线法给出的 T 终点系统性**早于**阈值法/小波法，差值典型 **10–20 ms**，且随 T 波对称性变化。做纵向随访或药物试验时，必须全程锁定同一方法与同一参数集。

### 4.4 阈值法（快速备用）

```
Toff = Tpk 之后，|y − B| 首次 < max( 0.10 · |Tpk − B| , 15 µV ) 的时刻   【工程】
去抖：需连续 3 个样点（6 ms @500Hz）满足才确认
```

最快但对基线最敏感，仅作为交叉验证的第三方法或极端情况兜底。

### 4.5 累积面积法

```
S_total = Σ |y(i) − B|,  i ∈ [Ton, Twin_end]
Toff = 累积面积首次达到 0.98 · S_total 的位置      【工程，0.97–0.985】
```

对噪声有天然平滑作用，但对搜索窗右端点敏感。

### 4.6 曲线拟合法（同时给出形态参数）

用非线性最小二乘（Levenberg–Marquardt）拟合参数化模型，一次拟合同时得到边界与形态量。**这是确定性曲线拟合，不属于机器学习。**

**双高斯模型**（适配单相与双相 T）：

```
T(t) = Σ_{j=1..J} a_j · exp( −(t − µ_j)² / (2σ_j²) )
J = 1（单相）或 2（双相/切迹）
```

- 初值：`a₁ = Tpk − B`，`µ₁ = Tpk`，`σ₁ = (Toff_wavelet − Ton_wavelet)/5`
- 边界解析定义：`Ton = µ₁ − 2.5σ₁`，`Toff = µ_J + 2.5σ_J` 【工程，k = 2.0–3.0 需标定】
- 拟合优度门限：`R² ≥ 0.95` 才采信；否则回退到小波结果 【工程】

**偏态高斯**（更贴合 T 波固有不对称）：

```
T(t) = a · exp(−(t−µ)²/(2σ²)) · [1 + erf( α(t−µ)/(σ√2) )]
```

参数 `α` 直接就是不对称度特征，正常窦性 T 的 `α` 典型为 **−1.5 ~ −0.5**（降支陡于升支）【工程】。

### 4.7 U 波判别与 T–U 融合处理

**T 终点的最大单一误差来源。**

**独立 U 波判据** 【工程】：
- 在 `[Toff + 60 ms, Toff + 200 ms]` 区间内
- 存在幅度 **20–150 µV** 的凸起
- 在低通 15 Hz 信号上，其一阶导数出现完整的「正 → 过零 → 负」序列
- 满足以上 → 判为独立 U 波，不影响 Toff

**T–U 融合判据** 【工程】：
- 在小波法求得的 `Toff` 处，`|dy/dt| > 5 µV/ms`（即 >10 µV/样点 @500 Hz）
- 或 `Toff` 之后 40 ms 内 `w4` 未回到噪声门限以下
- 满足以上 → 标记「T–U 融合」，降低置信度

**融合时的处置**：
在 `[x_s, x_s + 200 ms]` 内计算二阶导数 `d²y/dt²`（Savitzky-Golay 2 阶 / 11 点），取**曲率最大点**（`d²y/dt²` 首个显著正极大）作为 `Toff` 上界。该点对应 T 降支「拐平」的位置，通常早于 U 波起始。

**高危场景**：低钾血症、长 QT 综合征、心动过缓（HR <50）时 U 波显著，应强制启用融合检测。

### 4.8 方法选择决策树

```
读取 A = |Tpk − B|，形态类别 M，U 波标记 U

if T 波不可辨（小波模极大值 < 2 个）:
        → 返回 NaN，导联剔除

elif M == 双相 或 M == 切迹:
        → 小波法（唯一可靠）
        → 若拟合 R² ≥ 0.95，用双高斯结果交叉校验

elif U == 融合:
        → 曲率上界法 (§4.7) 与 梯形法 取【较早】者
        → 置信度降一级

elif A ≥ 200 µV:
        → 主：小波法
        → 校验：切线法。若两者差 > 25 ms → 置信降级
        → 输出：小波法结果

elif 100 µV ≤ A < 200 µV:
        → 小波法 + 梯形法，取两者【中位】（此时即中点）
        → 若两者差 > 30 ms → 置信降级

else  (A < 100 µV):
        → 梯形面积法（唯一可用）
        → 禁用切线法与阈值法
        → 置信度设为最低档；若 A < 50 µV 直接剔除该导联
```

---

## 5. 多导联：可靠导联筛选与稳健融合

### 5.1 派生「全局导联」

在原始导联之外，额外构造 3 条派生导联独立定界，作为融合的交叉校验。

**(a) RMS 导联**

```
对 8 个独立导联 (I, II, V1..V6)，各自先减去自身基线 B_l：
    RMS(k) = sqrt( (1/8) · Σ_l [x_l(k) − B_l]² )
```

RMS 导联的 T 波恒为正，形态单一，定界最稳定。用 §4.1 小波法处理，`ξ_off = 0.40`。

**(b) PCA 第一主成分**

```
1) 取 T 窗矩阵 X ∈ R^{8×N}，N = Twin 内样点数
2) 逐导联去均值（减自身在该窗内的均值）
3) SVD: X = UΣVᵀ
4) PC1 = U[:,0]ᵀ · X   → 1×N 时间序列
5) 在 PC1 上定界
```

PC1 通常承载 T 窗内 85–95% 的能量，SNR 显著优于任一原始导联。
副产品 `λ₂/λ₁ = (σ₂/σ₁)²` 即 T 波复杂度特征（§6）。

**(c) VCG 空间速度**

先由 12 导重建 XYZ。**Inverse Dower 变换矩阵**：

```
X = −0.172·V1 − 0.074·V2 + 0.122·V3 + 0.231·V4 + 0.239·V5 + 0.194·V6 + 0.156·I − 0.010·II
Y =  0.057·V1 − 0.019·V2 − 0.106·V3 − 0.022·V4 + 0.041·V5 + 0.048·V6 − 0.227·I + 0.887·II
Z = −0.229·V1 − 0.310·V2 − 0.246·V3 − 0.063·V4 + 0.055·V5 + 0.108·V6 + 0.022·I + 0.102·II
```

> Kors 回归变换在多数研究中优于 Inverse Dower（尤其 QRS-T 夹角类特征），建议实现时**直接从 Kors 原文取系数**核对，不要转抄二手来源。

空间速度：

```
SV(k) = sqrt( (dX/dt)² + (dY/dt)² + (dZ/dt)² )
导数用 Savitzky-Golay 2 阶 / 9 点

Toff_SV = SV 在 T 峰之后降到 0.15 · max(SV | T窗) 的时刻      【工程，0.10–0.20】
或取 T 波之后 SV 的第一个显著局部极小
```

SV 法最贴合「全局复极结束」的生理定义。

### 5.2 导联质量指标（SQI）与阈值

**通用 SQI**（逐导联、逐 10 s 段计算）：

| 指标 | 定义 | 合格阈值 | 说明 |
|---|---|---|---|
| kSQI | 信号峭度 kurtosis | **> 5** 好；< 4 判噪声 【文献】 | 正常 ECG 因 QRS 尖峰而高峭度 |
| sSQI | 偏度绝对值 \|skew\| | 参考区间 0.5–2.5 【工程】 | |
| pSQI | P(5–15 Hz) / P(5–40 Hz) | **0.5–0.8** 合格 【文献】 | QRS 能量占比 |
| basSQI | 1 − P(0–1 Hz)/P(0–40 Hz) | **> 0.95** 合格 【文献】 | 基线漂移占比 |
| bSQI | 两种不同 QRS 检测器的 R 峰一致率（±150 ms 内匹配） | **> 0.90** 【工程】 | 强指标 |
| ρ_tmpl | 与本导联中位拍模板的相关系数 | **> 0.90** 【工程】 | |
| 饱和率 | 满量程样点占比 | **< 0.1%** | |

**T 波专用可靠性指标**（这是筛选 T 定界可靠导联的核心）：

| 指标 | 定义 | 阈值 |
|---|---|---|
| `A_T` | \|Tpk − B\| | ≥ 200 µV 优；100–200 µV 中；50–100 µV 差；**< 50 µV 剔除** 【工程】 |
| `SNR_T` | 20·log₁₀( A_T / RMS(TP 段残差) ) | **> 10 dB** 优；6–10 dB 中；**< 6 dB 剔除** 【工程】 |
| `σ_beat` | 该导联跨拍 Toff（相对 R 峰）的标准差 | **< 8 ms** 优；8–20 ms 中；**> 20 ms 剔除** 【工程】 |
| `spread_alg` | 该导联上 2–3 种算法结果的极差 | **< 10 ms** 优；> 30 ms 剔除 【工程】 |
| `U_flag` | T–U 融合标记 | 置位则权重 ×0.5 |

### 5.3 导联权重

```
w_l = w_prior(l) · w_SQI(l) · w_amp(l) · w_consist(l)

w_amp     = min(1, A_T / 200 µV)
w_consist = min(1, 10 ms / max(spread_alg, 10 ms))
w_SQI     = 各 SQI 归一化后的几何平均（任一项不达标 → w_SQI = 0）
```

**导联先验权重 `w_prior`** 【工程，供起点使用，应在自有数据上重标定】：

| 导联 | 权重 | 理由 |
|---|---|---|
| V2, V3 | 1.00 | T 幅度最大，SNR 最优 |
| V4, V5 | 0.95 | 幅度大且形态稳定；V5 为临床 QT 首选导联 |
| II | 0.90 | 临床 QT 首选导联，T 形态典型 |
| V6 | 0.85 | |
| I | 0.80 | 幅度中等 |
| aVF | 0.75 | |
| aVR | 0.70 | 需先极性翻转（乘 −1） |
| V1 | 0.60 | 常双相，U 波易混入 |
| III, aVL | 0.55 | 幅度常过小，方向不稳 |
| **RMS（派生）** | 1.00 | |
| **PC1（派生）** | 1.00 | |
| **SV（派生）** | 0.90 | 与其余高度相关，避免过权重 |

**硬性剔除规则**（不进入融合）：
- `A_T < 50 µV`
- `SNR_T < 6 dB`
- 任一通用 SQI 不达标
- 该导联 T 波不可辨（小波模极大值 <2 个）

### 5.4 稳健融合算法

**前提**：所有导联必须先按**同一个全局 R 峰**对齐，使各导联的 `Toff_l` 落在同一时间轴上。融合的是相对 R 峰的偏移量 `d_l = Toff_l − R`。

```
输入：{ d_l }, { w_l },  l ∈ 保留导联集 L

Step 1  初始估计
        m₀ = weighted_median(d, w)

Step 2  稳健尺度
        MAD = 1.4826 · median( |d_l − m₀| )
        σ̂  = max(MAD, 3 ms)                      # 下限防退化

Step 3  内点判定
        Inliers = { l : |d_l − m₀| ≤ max(3·σ̂, 15 ms) }      【工程】

Step 4  Huber IRLS（仅用内点），c = 1.345·σ̂
        repeat (最多 5 次，或位移 < 0.5 ms 时停):
            r_l  = d_l − m
            u_l  = w_l · (1 if |r_l| ≤ c else c/|r_l|)
            m    = Σ u_l·d_l / Σ u_l

Step 5  输出
        Toff_global = R + m
        标准误 SE   = σ̂ / sqrt( Σ_{Inliers} w_l )
        95% CI      = ± 1.96 · SE
```

**关于「全局 T 终点」的两种定义** —— 必须显式选择并在文档中固定：

| 定义 | 计算 | 适用 |
|---|---|---|
| **稳健中心** | 上述 IRLS 结果 `m` | 与人工判读（II / V5）对齐、算法评测、常规特征提取 |
| **最晚复极** | 内点集合中 `d_l` 的 **P85 分位数**（**不要用 max**） | 符合「全局 QT = 最早除极 → 最晚复极」的生理/标准定义 |

直接取 `max` 对单个噪声导联极其敏感，工程上不可用；P85 是精度与生理定义之间的实用折中。

同理，全局 `QRSon` 取内点的 **P15 分位数**（而非 min），全局 `J` 取内点的 **P85 分位数**。

### 5.5 融合结果质控门限 【工程】

| 检查项 | 门限 | 处置 |
|---|---|---|
| 内点数 | < 4 | 该拍/该记录作废 |
| 内点 MAD | > 20 ms | 置低置信 |
| 派生导联（RMS/PC1/SV）与融合值偏差 | > 25 ms | 报警，人工复核 |
| 三条派生导联互相极差 | > 30 ms | 报警 |
| 全局 QT | < 250 ms 或 > 700 ms | 生理性不可信，作废 |
| QTc（Fridericia） | < 300 ms 或 > 600 ms | 复核 |

---

## 6. T 波形态特征

所有特征在**中位拍**上计算；需要变异性时另行逐拍计算。

### 6.1 单导联时域特征

| 特征 | 定义 | 单位 | 参考范围 |
|---|---|---|---|
| `T_amp` | `y(Tpk) − B` | µV | V2–V5 正常 200–900；肢导 100–400 【临床】 |
| `T_area` | `Σ (y(i) − B) / fs`，i ∈ [Ton, Toff] | µV·s | 极性敏感 |
| `T_absarea` | `Σ \|y(i) − B\| / fs` | µV·s | |
| `T_dur` | `Toff − Ton` | ms | 正常 **150–250 ms** 【临床】 |
| `TpTe` | `Toff − Tpk` | ms | 正常 **< 100 ms**（V5）；> 110 ms 异常 【临床】 |
| `TpTe/QT` | 比值 | — | 正常 **0.15–0.25**；> 0.28 异常 【临床】 |
| `T_asc` / `T_desc` | `Tpk − Ton` / `Toff − Tpk` | ms | |
| `SymR_time` | `T_asc / T_desc` | — | 正常 T 不对称，典型 **0.5–0.7**；趋近 1.0 提示缺血/高钾 【工程】 |
| `SymR_slope` | \|升支最大斜率\| / \|降支最大斜率\| | — | 正常 **0.5–0.8** 【工程】 |
| `T_flat` | `T_amp / T_dur` | µV/ms | 平坦度 |

### 6.2 形状矩（在 `[Ton, Toff]` 内，以 `\|y−B\|` 归一化为概率密度）

```
p(i) = |y(i) − B| / Σ|y − B|
µ₁ = Σ i·p(i)                       # 重心时刻
m_k = Σ (i − µ₁)^k · p(i)
Skew = m₃ / m₂^1.5                  # 正常窦性 T 典型 0.2 ~ 0.6   【工程】
Kurt = m₄ / m₂²                     # 正常典型 2.0 ~ 3.0          【工程】
```

### 6.3 切迹与多相判定

在 `x_t`（低通 **15 Hz**，必须充分低通，否则噪声制造伪极值）上：

```
Notch_count = [Ton, Toff] 内一阶导数过零点数 − 1
判定为切迹 T：存在次级极值，其幅度 ≥ 0.10 · T_amp 且距主峰 ≥ 30 ms   【工程】
判定为双相 T：存在跨越基线的极性反转，反向峰幅度 ≥ 50 µV           【工程】
```

### 6.4 多导联/空间特征（仅多导联可得）

| 特征 | 定义 | 参考范围 |
|---|---|---|
| **PCA ratio** | `λ₂/λ₁`，T 窗 8 导 SVD 奇异值平方比 | 正常 **< 0.20**；> 0.30 提示复极异常 【工程】 |
| **TWR**（T-wave Residuum） | `sqrt(Σ_{i=4..8} λ_i)`，前 3 主成分之外的残余能量 | 正常低值；相对量纲，需自标定 |
| **TMD**（T-wave Morphology Dispersion） | 各导联 T 波在前 2 主成分平面上重建向量两两夹角的均值 | 正常 **< 30°**；> 50° 异常 【文献】 |
| **TCRT**（Total Cosine R-to-T） | QRS 主向量与 T 主向量夹角余弦（在 VCG 主成分空间） | 正常 **> 0.5**；< 0 显著异常 【文献】 |
| **空间 QRS-T 夹角** | VCG 中 QRS 平均向量与 T 平均向量的三维夹角 | 正常 **< 75°**；临界 75–105°；异常 **> 105°** 【临床】 |
| **T 环平面性** | T 环最佳拟合平面的残差 / 环长 | 越小越平面 |
| **T 环面积 / 离心率** | 主平面上环的面积与长短轴比 | |

**空间 QRS-T 夹角计算**：

```
QRS 平均向量 = (1/N₁)·Σ_{k∈[QRSon,J]} [X(k), Y(k), Z(k)]
T   平均向量 = (1/N₂)·Σ_{k∈[Ton,Toff]} [X(k), Y(k), Z(k)]
angle = arccos( (v_QRS · v_T) / (|v_QRS|·|v_T|) ) · 180/π
```

### 6.5 逐拍动态特征（需逐拍定界）

**T 波电交替（TWA）—— 谱方法**：

```
1) 取连续 128 拍（必须窦性、RR 变异 < 10%）
2) 对 T 窗内每个时间样点，构造跨拍序列（长度 128）
3) 加 Hanning 窗，FFT
4) TWA 幅度 = 0.5 Hz 处（Nyquist，即每两拍交替）的谱峰
5) 噪声估计 = 0.43–0.47 Hz 频段的平均功率
6) K-score = (峰值 − 噪声均值) / 噪声标准差
   判阳性：K ≥ 3.0 且 TWA 电压 ≥ 1.9 µV        【文献】
```

**MMA（改良移动平均）法**：奇偶拍分别做指数移动平均，更新系数 **1/8**（即新拍权重 12.5%）【文献】，TWA = 奇偶平均拍在 ST-T 段的最大绝对差。抗噪优于谱法，不要求严格窦律。

**QT 变异性**：`QTVI = log10[ (QTv/QTm²) / (RRv/RRm²) ]`，正常约 **−1.9 ~ −0.9** 【文献】。

---

## 7. ST 段特征

### 7.1 测量点定义

所有 ST 电平相对同一拍的等电位基线 `B`（§2.2）。

```
STj   = y(J)        − B
ST20  = y(J + 20ms) − B
ST40  = y(J + 40ms) − B
ST60  = y(J + 60ms) − B
ST80  = y(J + 80ms) − B
```

**心率自适应测量点**（运动试验惯例）【文献】：

| HR (bpm) | 主测量点 |
|---|---|
| < 100 | J + 80 ms |
| 100–110 | J + 72 ms |
| 110–120 | J + 64 ms |
| > 120 | J + 60 ms |

或使用连续公式：`t_meas = J + min(80 ms, 0.10·RR)`。

**安全边界**：`t_meas` 必须早于 `Ton − 20 ms`，否则测到的是 T 波上升支而非 ST 段。心率过快时优先缩短到 J+40 ms 并标记。

### 7.2 斜率与形状

**ST 斜率**：在 `[J + 20 ms, J + 80 ms]` 上做最小二乘线性回归，取斜率 `k_ST`（µV/ms，等价于 mV/s）。

分类阈值 【工程】：

| 形态 | `k_ST` |
|---|---|
| 上斜型 | > +0.5 µV/ms |
| 水平型 | −0.5 ~ +0.5 µV/ms |
| 下斜型 | < −0.5 µV/ms |

**ST 曲率（凹/凸）**：在 `[J, J + 100 ms]` 上二次拟合 `y = a·t² + b·t + c`

- `a > 0`：凹面向上（concave up）—— 早期复极、心包炎的典型形态
- `a < 0`：凸面向上（convex up）—— 急性心肌损伤的典型形态
- 判定阈值：`|a| > 0.02 µV/ms²` 才判定形态，否则判为直线型 【工程】

**ST 面积**：`ST_area = Σ (y(i) − B) / fs`，`i ∈ [J, Ton]`，单位 µV·s。

**ST-T 联合量**：
- `ST_T_area`：`[J, Toff]` 积分
- `ST_T_ratio = STj / T_amp`
- `ST_integral_norm = ST_area / (Ton − J)`（平均 ST 电平）

### 7.3 临床参考阈值（仅作特征标定参考，不作诊断输出）

**ST 抬高**（J 点处，需 ≥2 个解剖相邻导联）【临床，第四版心肌梗死通用定义】：

| 导联 | 人群 | 阈值 |
|---|---|---|
| V2–V3 | 男 ≥40 岁 | ≥ 200 µV |
| V2–V3 | 男 <40 岁 | ≥ 250 µV |
| V2–V3 | 女性（全年龄） | ≥ 150 µV |
| 其他所有导联 | 全部 | ≥ 100 µV |
| V3R–V4R | 全部 | ≥ 50 µV（男 <30 岁 ≥100 µV） |
| V7–V9 | 全部 | ≥ 50 µV |

**ST 压低**：新发水平型或下斜型 **≥ 50 µV**，≥2 个相邻导联 【临床】。运动试验常用阈值为 **≥ 100 µV** 水平/下斜型。

### 7.4 ST 向量（VCG 域）

```
ST 向量 = [X(t_meas), Y(t_meas), Z(t_meas)] − [X_B, Y_B, Z_B]

幅值   = |v_ST|
方位角 azimuth   = atan2(Z, X) · 180/π      # 水平面
仰角   elevation = atan2(Y, sqrt(X²+Z²)) · 180/π
```

ST 向量方向对缺血区域定位的信息量远高于单导联阈值判断。

### 7.5 多导联 J 点融合

J 点误差**线性传递**到所有 ST 特征，是 ST 分析的首要误差源。

- 各导联独立定 `J_l`（§2.1），统一到全局 R 峰时间轴
- 用 §5.4 同款稳健流程，但**输出取内点的 P85 分位数**（不同导联 QRS 结束时刻本就不同，全局 J 应对应最晚者）
- 质控：内点极差 > 15 ms → ST 特征全部降级

---

## 8. 精度预算与验证

### 8.1 CSE 库容差标准（2SD 上限）【文献】

| 基准点 | 容差 |
|---|---|
| P onset | 10.2 ms |
| P offset | 12.7 ms |
| QRS onset | 6.5 ms |
| QRS offset | 11.6 ms |
| **T offset** | **30.6 ms** |

T 终点容差最宽，正说明它是全流程最难的点。

### 8.2 验证数据集

| 数据集 | 内容 | 用途 |
|---|---|---|
| **QTDB**（PhysioNet QT Database） | 105 条 2 导记录，约 3600 拍，2 位心脏科医师标注 | T 定界主基准 |
| **CSE Multilead** | 多导联同步标注 | 多导联融合验证、容差对标 |
| **LUDB** | 200 条 12 导，全导联 P/QRS/T 逐波定界标注 | **多导联方法首选** |
| **PTB-XL** | 大规模 12 导，含诊断标签 | 形态特征分布统计、鲁棒性压测 |
| **European ST-T Database** | 90 条，ST 与 T 变化逐拍标注 | ST 特征验证 |

### 8.3 报告规范

- 报告**有符号误差**的 `mean ± SD`（暴露系统偏倚），不能只报 MAE
- 分层报告：按 T 幅度分层（<100 / 100–200 / >200 µV）、按形态分层（单相/双相/切迹/倒置）、按心率分层
- 同时报告**检出率**（Se / PPV）：漏检的拍不能从误差统计中静默剔除
- 对每个导联单独报告一次，再报告融合后结果 —— 融合必须带来可见的 SD 下降，否则融合逻辑有问题

### 8.4 目标性能参考

| 项 | 单导联（好导联） | 多导联融合目标 |
|---|---|---|
| T offset 误差 SD | 15–20 ms | **< 12 ms** 【工程】 |
| T peak 误差 SD | 6–10 ms | < 6 ms |
| J 点误差 SD | 8–12 ms | < 8 ms |
| T 检出率 | > 98% | > 99.5% |

---

## 9. 常见误差来源清单

| # | 误差源 | 典型量级 | 规避 |
|---|---|---|---|
| 1 | **U 波混入 / T–U 融合** | 20–60 ms | §4.7 判别；低钾、缓脉、长 QT 时强制启用 |
| 2 | **0.5 Hz 高通造成的 ST 扭曲** | 50–100 µV | 用中值滤波或 0.05 Hz 零相位 |
| 3 | **方法间系统偏倚**（切线 vs 阈值） | 10–20 ms | 全流程锁定单一方法与参数 |
| 4 | **等电线选取** | 20–50 µV | PR 段中位数 + 平坦度检查 |
| 5 | **低幅 T 用切线法** | > 40 ms | `A_T < 100 µV` 时禁用切线法 |
| 6 | **J 点误差传递到 ST** | 与斜率成正比 | 多导联 P85 融合；宽 QRS 时降级 |
| 7 | **PVC/融合波混入中位拍** | 不定，可极大 | 形态聚类必须在中位拍之前 |
| 8 | **高心率 P-on-T** | 30–80 ms | 搜索窗截断到 `nextQRSon − 120 ms` |
| 9 | **搜索窗右端点过短** | 系统性偏早 | 用 RR 中位数而非瞬时 RR |
| 10 | **低采样率栅格量化** | ±2–4 ms | 插值到 1000 Hz |
| 11 | **房颤 RR 剧烈变异** | 窗口塌缩 | RR 用 5 拍中位数；房颤时逐拍重算窗 |
| 12 | **导联极性未归一（aVR）** | 形态判定错误 | aVR 统一乘 −1 后再入融合 |

---

## 10. 参数速查表

| 参数 | 符号 | 推荐值 | 类别 |
|---|---|---|---|
| 采样率 | fs | 500 Hz（QT 精测 1000 Hz） | 工程 |
| 基线中值滤波窗 | W1 / W2 | 200 ms / 600 ms | 文献 |
| 零相位高通截止 | fc | 0.05 Hz, Butterworth 4 阶 | 文献 |
| T 分支低通 | — | 20–25 Hz（U 波判别用 15 Hz） | 工程 |
| 幅度分支低通 | — | 40 Hz | 工程 |
| 拍对齐搜索范围 | — | ±30 ms | 工程 |
| 聚类相关阈值 | ρ | 0.90 | 工程 |
| 中位拍最少拍数 | — | 8 | 工程 |
| 小波尺度（T） | — | 2⁴ @500Hz；2⁵ @1000Hz | 文献 |
| 小波噪声门限 | ε_T | max(0.25·RMS(w4), 0.125·max\|w4\|) | 文献 |
| T onset 阈值 | ξ_on | 0.25 | 文献 |
| T offset 阈值 | ξ_off | 0.40（低幅时 0.50） | 文献 |
| 边界最大搜索距离 | — | Toff ≤ 160 ms；Ton ≤ 120 ms | 工程 |
| 梯形法参考点 | x_r | x_m + 160 ms | 工程 |
| 切线法最低 T 幅 | — | 100 µV | 工程 |
| 阈值法比例 | — | max(0.10·A_T, 15 µV) | 工程 |
| 累积面积比例 | — | 0.98 | 工程 |
| 高斯拟合边界系数 | k | 2.5σ | 工程 |
| 拟合优度门限 | R² | 0.95 | 工程 |
| T 搜索窗起 | — | J + max(60 ms, 0.04·RR) | 工程 |
| T 搜索窗止 | — | J + min(0.62·RR, 500 ms) | 工程 |
| 导联硬剔除幅度 | A_T | < 50 µV | 工程 |
| 导联硬剔除 SNR | SNR_T | < 6 dB | 工程 |
| 跨拍稳定性上限 | σ_beat | 20 ms | 工程 |
| 内点判定 | — | max(3·MAD, 15 ms) | 工程 |
| MAD 尺度下限 | — | 3 ms | 工程 |
| Huber 常数 | c | 1.345·σ̂ | 文献 |
| 最少内点数 | — | 4 | 工程 |
| 「最晚复极」分位数 | — | P85（**不用 max**） | 工程 |
| SV 法阈值 | — | 0.15·max(SV) | 工程 |
| ST 主测量点 | — | J + min(80 ms, 0.10·RR) | 文献 |
| ST 斜率窗 | — | [J+20 ms, J+80 ms] | 工程 |
| ST 形态分界斜率 | k_ST | ±0.5 µV/ms | 工程 |
| ST 曲率判定门限 | \|a\| | 0.02 µV/ms² | 工程 |
| kSQI 合格 | — | > 5 | 文献 |
| pSQI 合格 | — | 0.5–0.8 | 文献 |
| basSQI 合格 | — | > 0.95 | 文献 |
| bSQI 合格 | — | > 0.90 | 工程 |

---

## 11. 完整流程伪代码

```python
def delineate_12lead(sig12, fs):
    # ---------- 1. 前处理 ----------
    if fs < 500: sig12 = resample(sig12, 1000); fs = 1000
    x = remove_baseline_median(sig12, fs, w1=0.200, w2=0.600)
    x = remove_artifacts(x, fs)                  # 起搏尖峰/饱和/平线
    x_qrs = bandpass(x, 5, 15, fs)
    x_fid = lowpass_zerophase(x, 40, fs)
    x_t   = lowpass_zerophase(x, 22, fs)
    x[aVR] *= -1                                 # 极性归一

    # ---------- 2. 全局 R 峰 ----------
    R = fuse_qrs_detections(x_qrs, fs)           # 多导联投票
    RR = median_filter(diff(R), 5)

    # ---------- 3. 中位拍 ----------
    clusters = cluster_beats(x_qrs, R, rho=0.90, dwidth=0.025)
    med = {l: median_beat(x_t[l], clusters.main) for l in LEADS}
    if len(clusters.main) < 8: flag('LOW_BEAT_COUNT')

    # ---------- 4. 基准点与基线 ----------
    QRSon = {l: wavelet_qrs_onset(x_fid[l]) for l in LEADS}
    J     = {l: wavelet_j_point(x_fid[l])   for l in LEADS}
    B     = {l: isoelectric(x[l], QRSon[l], RR) for l in LEADS}
    QRSon_g = robust_quantile(QRSon, weights, q=0.15)
    J_g     = robust_quantile(J,     weights, q=0.85)

    # ---------- 5. 派生导联 ----------
    rms  = rms_lead(x_t, B, INDEP8)
    pc1  = pca_first_component(x_t, INDEP8, T_window)
    svel = spatial_velocity(inverse_dower(x_t), fs)

    # ---------- 6. 逐导联定界 ----------
    res = {}
    for l in LEADS + ['RMS', 'PC1', 'SV']:
        win  = t_search_window(J[l], RR)
        wav  = wavelet_delineate_T(med[l], win, fs,
                                   xi_on=0.25, xi_off=0.40)
        if wav is None: res[l] = None; continue
        A_T  = abs(wav.Tpk_amp - B[l])
        U    = detect_u_wave(med[l], wav.Toff, fs)
        res[l] = select_method(wav, med[l], B[l], A_T, U, fs)   # §4.8
        res[l].sqi = compute_sqi(x[l], med[l], A_T, fs)

    # ---------- 7. 筛选 + 稳健融合 ----------
    keep = [l for l in res
            if res[l] and res[l].sqi.pass_all
            and res[l].A_T >= 50 and res[l].snr_T >= 6]
    if len(keep) < 4: return FAIL('INSUFFICIENT_LEADS')

    w = {l: w_prior[l] * res[l].sqi.score
            * min(1, res[l].A_T/200)
            * consistency_weight(res[l]) for l in keep}

    Toff_g, ci, inliers = robust_fuse([res[l].Toff for l in keep], w)   # §5.4
    Ton_g,  _,  _       = robust_fuse([res[l].Ton  for l in keep], w)

    # ---------- 8. 质控 ----------
    qc = quality_check(inliers, Toff_g, res['RMS'], res['PC1'], res['SV'])

    # ---------- 9. 特征提取 ----------
    feats = {}
    feats.update(t_morphology(med, B, Ton_g, Toff_g, res))       # §6
    feats.update(spatial_features(inverse_dower(x_t), QRSon_g,
                                  J_g, Ton_g, Toff_g))          # §6.4
    feats.update(st_features(med, B, J, RR, Ton_g))              # §7

    return Result(QRSon_g, J_g, Ton_g, Toff_g, feats, ci, qc)
```

---

## 12. 实施建议

1. **先做基线版本**：中位拍 + 小波定界 + SQI 加权中位数融合。在 LUDB 与 QTDB 上量化误差分布（分层报告）。这一步就能达到 T offset SD ≈ 15 ms。
2. **再加梯形法与决策树**：低幅 T 是剩余大误差的主要来源，加入 §4.2 与 §4.8 后 SD 通常可降到 12 ms 以内。
3. **最后加 U 波处理与派生导联交叉校验**：主要贡献是把长尾（>50 ms 的大误差）压下去，对 SD 的改善有限但对最坏情况改善显著。
4. **所有 `【工程】` 阈值必须在自有数据上重标定一次**，尤其是导联先验权重、SQI 门限、`ξ_off`、梯形法 `x_r`。不同的采集设备、电极位置、人群构成会明显移动这些最优值。
5. **参数一经确定不得中途变更**。若必须变更，所有历史数据必须重算，否则纵向比较失效。