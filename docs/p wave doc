<!-- i18n-nav -->
[中文](p%20wave%20doc) | [English](p%20wave%20doc.en.md) | [日本語](p%20wave%20doc.ja.md)
<!-- /i18n-nav -->

# 多导联 ECG P 波定界：代码修改重点指南

> 适用范围：不使用机器学习，通过确定性信号处理实现多导联 P 波 onset/offset 定界。  
> 工程目标：优先保证结果可靠、可解释、可校准，并在信号不可辨识时明确拒判。

---

## 1. 核心设计

推荐采用以下主流程：

**采集质量检查 → 双分支预处理 → 心房活动状态判断 → 多导联粗定位 → 单导联精定界 → 可靠性评估 → 稳健融合 → 拍间校验**

其中最重要的原则是：

1. **先用多导联信息确定 P 波的大致范围，再在窄窗内逐导联精定位。**
2. **等电位或高噪声导联不参与定界和融合。**
3. **全局 onset/offset 是“最早或最晚可信边界”，不是所有导联边界的平均值。**
4. **Ta 波是真实的心房复极活动，不能简单当作基线或噪声去除。**
5. **高心率下 TP 段消失时，必须切换基线估计模式。**
6. **P-on-T 场景不能使用固定 T 模板直接硬相减。**
7. **无法可靠区分 P、T、Ta 或伪差时，必须允许拒判。**

---

## 2. 输出内容

系统不应只输出一个 onset 和 offset。每拍至少输出：

- 严格全局 onset/offset
- 稳健支持 onset/offset
- onset/offset 置信区间
- P 波状态
- 有效导联列表
- 各导联边界、质量评分和不确定度
- 当前使用的基线模式
- 是否经过 T 波重建
- 是否存在 Ta 波歧义
- 接受或拒判状态
- 拒判原因

### 两套全局边界

| 输出 | 含义 | 用途 |
|---|---|---|
| 严格全局边界 | 通过硬质控的导联中，最早 onset 和最晚 offset | 接近传统多导联测量口径 |
| 稳健支持边界 | 获得多个相对独立导联组支持的最早/最晚可信边界 | 工程主输出 |

两套结果相差过大时，应降低置信度。常见原因包括单导联伪差、通道不同步、T/Ta 残余或导联接错。

### 建议的 P 波状态

- `P_PRESENT`：检测到稳定 P 波
- `AF_LIKE`：无稳定 P 模板，表现接近房颤
- `ORGANIZED_ATRIAL_ACTIVITY`：房扑或多个有组织心房波
- `OVERLAP_UNCERTAIN`：P-on-T、Ta 干扰或证据不足

### 建议的拒判原因

- `BASELINE_UNOBSERVABLE`
- `P_ON_T_UNRESOLVED`
- `P_OFFSET_TA_AMBIGUOUS`
- `INSUFFICIENT_INFORMATIVE_LEADS`
- `CROSS_LEAD_DISAGREEMENT`
- `CHANNEL_DESYNCHRONIZED`
- `LEAD_CONFIGURATION_INVALID`
- `MODEL_DISAGREEMENT`

---

## 3. 采集链与通道质量检查

该步骤必须放在全部滤波和定界之前。

### 必查项目

- 采样率、时间戳和数据长度是否一致
- 信号单位和增益是否正确
- 是否存在饱和、平线、导联脱落或大尖峰
- 各导联是否同步
- 各通道是否经过不同的滤波或重采样
- 是否存在半采样点或固定通道延迟
- 肢体导联之间的代数关系是否合理
- 是否存在导联反接、重复通道或异常增益

### 修改重点

- 增加通道延迟估计和补偿。
- 利用 QRS 高斜率区域检查跨导联时间同步。
- 增加肢体导联关系检查，用于发现接错、增益异常和不同步。
- 发现采集链错误时，不应继续进行多导联融合。

---

## 4. 双分支预处理

不要让同一套强低通信号同时承担粗检测和最终精定界。

### 检测分支

用于：

- QRS/T 检测
- P 波粗定位
- 空间速度计算
- 模板相关
- 心房活动状态判断

建议采用较强的低通，以提高整体稳定性。

### 精定界分支

用于：

- 最终 onset/offset 定位
- 小切迹和碎裂 P 检查
- 局部变化点检测
- 宽带结果交叉验证

建议保留较宽的诊断带宽，避免强低通抹掉微弱的初始或终末成分。

### 上采样说明

上采样可以：

- 减少输出被锁定在原始采样栅格上的问题
- 统一不同采样率下的时间窗配置
- 支持切线交点和连续曲线拟合

但它**不能恢复采集阶段已经丢失的信息**。

---

## 5. 基线估计：普通模式与无 TP 模式

### 普通模式

PQ 或 TP 段中的点不能无条件作为“零电位节点”。只有同时满足下列条件时，才可作为基线软观测：

- 多导联活动度低
- 局部斜率和细尺度能量低
- 不处于 P、QRS 或 T 活动区
- T/Ta 残余较小
- 多导联没有同步变化

这些点应作为带不确定度的软约束，而不是强制钉死的零电位。

### 高心率、TP 消失模式

TP 消失意味着基线暂时缺少直接观测，不能继续逐拍自由样条拟合。

建议：

- 使用跨多拍的状态空间基线模型。
- 有可信低活动样本时更新基线。
- 没有可信样本时只进行预测。
- 随着无观测时间增长，自动增大基线不确定度。
- 离线处理可使用双向平滑；在线处理使用固定延迟平滑。

### 多导联基线

优先把基线分成：

- 多导联共享的慢变部分，例如呼吸或体动
- 各导联自身的缓慢残余

这样比每个导联独立使用高自由度样条更不容易把真实 P 波吸收到基线中。

### 失败处理

若长期不存在可信等电位观测，且基线、T 尾、Ta 尾与 P 波无法可靠分离，应输出：

`BASELINE_UNOBSERVABLE`

---

## 6. Ta 波感知

Ta 波可能影响当前 P 波的 offset，也可能在高房率时延续到下一拍 P onset 附近。

### 主要风险

- 上一拍 Ta 尾被误认为下一拍 P 的早期活动
- 当前 Ta 起始部分改变 P 波回零形态
- PQ/TP 段不再是真正等电位
- 面积法或基线交点法产生系统偏差
- 稳定的 Ta 尾被误判为高置信终末 P

### 代码处理原则

- Ta 波只作为受限制的慢变干扰项处理，不追求逐拍完整重建。
- 允许使用少量平滑基函数描述 Ta/T 尾部。
- P 波核心区禁止 Ta 模型自由拟合。
- Ta 与 P 反极性只能作为软提示，不能作为硬规则。
- 扰动 Ta 的强度、宽度和位置，将边界变化计入不确定度。

### Onset 与 offset 分开处理

P onset 可以在慢变背景上寻找局部活动开始点，因此通常比 offset 更容易保持稳定。

P offset 更容易受到 Ta 影响。进入 Ta-aware 模式后：

- 面积回零法降权或停用
- 基线交点法降权
- 提高细尺度小波、导数活动度和空间方向连续性的权重
- 多种方法明显分歧时，不输出高置信边界

无法区分终末 P 和早期 Ta 时，输出：

`P_OFFSET_TA_AMBIGUOUS`

---

## 7. P-on-T：受约束的 T 波重建

### 禁止固定模板直接硬相减

直接相减的主要问题包括：

- T 波幅度逐拍变化，留下明显低频残余
- T 波时间轻微错位，产生尖锐的导数形残余
- 矩形窗启停造成边缘不连续和宽带伪影
- 固定 PR 节律下，P 波可能被写入 T/QRST 模板并一起减掉

### 推荐方式

将“模板相减”改为“受约束的多导联 T 波重建”：

- P 候选区不参与 T 波参数估计
- T 模板来自当前记录的相似搏动
- 模板按 QRS/T 形态、RR、呼吸相位和空间方向筛选
- 只允许有限的幅度缩放、时间平移、轻微伸缩和空间调整
- 禁止无约束时间扭曲
- 重建结果必须保持平滑连续
- 禁止矩形窗硬拼接

### 三分支交叉验证

同时保留三个分支：

1. **原始宽带分支**：不相减，只处理慢变背景。
2. **T 波重建分支**：使用受约束重建后的残差信号。
3. **T 空间子空间抑制分支**：从多导联空间方向上抑制 T 波。

判断规则：

- 三个分支结果一致：提高置信度
- 结果小范围分歧：扩大置信区间
- 结果明显分歧：输出 `P_ON_T_UNRESOLVED`

### T 重建失败检查

必须检查：

- 重建作用区边缘是否出现新的尖峰
- 无 P 的 T 区残差是否确实下降
- 更换相近模板后边界是否稳定
- 重建后是否削弱了已知 P 波形态
- 是否产生原始宽带信号完全不支持的新边界
- 新边界是否获得多个独立导联组支持

---

## 8. 多导联粗定位

### 主方法：白化空间活动度

普通多导联平方和默认各导联噪声独立且方差接近，实际通常不成立。

建议先根据低活动区估计多导联噪声协方差，再计算白化后的空间活动度，以减轻：

- 单个高噪导联
- 跨导联共模伪差
- 肢体导联冗余
- 不同导联噪声幅度差异

阈值优先从低活动区的经验分布获得，并采用高低双阈值：

- 高阈值确认存在活动
- 低阈值回溯真正的起点或终点
- 要求活动持续一定时间，避免单点噪声触发

### SVD 互补分支

不要始终只使用第一主成分。

建议：

- 先按噪声水平白化
- 再按导联质量加权
- 当第一和第二空间分量接近时，保留二维子空间
- 空间方向不稳定时提高不确定度
- 最多进行少量迭代，防止错误初始窗自我强化

SVD 结果只能解释为有利的体表空间投影，不能解释为解剖学激动起点。

---

## 9. 单导联精定界

单一方法不应独立决定最终边界。

### Onset 主证据

- 多尺度小波
- 局部变化点
- 最小二乘切线
- 宽带与检测分支的一致性

### Offset 主证据

- 多尺度小波活动消失
- 导数活动度回落
- 条件式面积法
- 受约束曲线拟合
- Ta-aware 结果

### 关键修改

- 双相和三相 P 波必须显式处理，尤其是 V1。
- 不使用“第一个极值就是 onset”之类固定规则。
- 切线法应对最大斜率附近的一小段进行直线拟合，避免使用单点导数。
- 面积法只在基线可靠、无明显 Ta、无严重 P-on-T 时启用。
- 曲线拟合主要用于亚采样估计和交叉验证，不能强行解释所有复杂形态。
- 多尺度或多方法分歧必须进入不确定度计算。

---

## 10. 导联可靠性

### 硬排除

- 平线
- 饱和
- 导联脱落
- 大幅尖峰
- 严重不同步
- 明显增益异常
- 导联配置错误

### 软质量指标

- P 波局部信噪比
- P 波局部能量
- 导数能量
- 与本记录模板的相关性
- 基线稳定度
- 多尺度一致性
- 多方法一致性
- 阈值扰动稳定性
- 拍间边界抖动
- T 重建敏感性
- Ta 模型敏感性

### 等电位导联

不能只看单个峰值。应联合检查：

- 峰值是否显著
- 总能量是否显著
- 导数能量是否显著
- 是否存在稳定模板结构
- 多尺度下是否存在一致活动

多个证据均表明没有有效 P 信息时：

- 标记为无信息导联
- onset/offset 权重置零
- 不输出该导联边界

### 生理范围

P 波时限或幅度超出常见范围可能是真实病理，不应一律硬剔除。

- 采集故障：硬排除
- 生理范围越界：软惩罚
- 病理形态：允许保留，但提高不确定度

---

## 11. 不确定度与置信区间

阈值扰动只能反映部分不稳定性，不能单独代表真实置信度。

不确定度至少应覆盖：

- 高频随机噪声
- 基线估计误差
- 小波尺度和阈值变化
- 导数窗口变化
- 不同定界方法分歧
- T 模板选择和重建参数变化
- Ta 模型变化
- 拍间抖动

### 实现要点

- 对相关参数做小范围合理扰动，重复定界。
- 噪声重采样应保留时间相关性和跨导联相关性。
- 给每个导联设置最小不确定度，避免无限大权重。
- 给单导联和导联组设置最大权重，避免单一路径支配融合。
- 最终输出置信区间，而不只是一个抽象质量分数。

### 校准检查

在验证集上检查：

- 标称置信区间是否达到相应实际覆盖率
- 置信区间越宽时，真实误差是否总体越大
- 低置信样本被拒判后，已接受结果是否明显改善

---

## 12. 单侧稳健融合

全体导联的加权中位数不适合作为最终全局边界，因为它容易推迟 onset、提前 offset，从而缩短 P 波时限。

### Onset 融合

- 将导联 onset 按时间排序
- 在有限时间范围内形成候选时间簇
- 要求至少两个相对独立的导联组支持
- 选择最早达到可靠权重门限的时间簇
- 在该簇内部取偏早的加权位置

### Offset 融合

- 使用相同原则寻找最晚可信时间簇
- 在簇内取偏晚的加权位置

### 导联分组

至少区分：

- 肢体导联组
- 右胸前导联组
- 左胸前导联组

每组设置权重上限，避免相关噪声被重复计票。

### 拒判条件

剔除明显离群导联后，如果边界仍高度分散，应输出：

`CROSS_LEAD_DISAGREEMENT`

---

## 13. 拍间一致性

只能在相同形态簇内进行拍间平滑或叠加。

需要分开的形态至少包括：

- 窦性 P
- 房性早搏
- 逆行 P
- 其他稳定异位 P

禁止跨形态簇进行：

- 叠加平均
- PR 间期平滑
- P 时限平滑
- 边界 Kalman 平滑

突变首先应触发形态或节律模式切换检查，不应直接被平滑掉。

---

## 14. 建议的代码模块

| 模块 | 主要职责 |
|---|---|
| `acquisition_qc` | 通道同步、增益、导联关系、饱和和脱落检查 |
| `preprocessing` | 检测与精定界双分支滤波、重采样 |
| `baseline` | 普通基线和无 TP 状态空间基线 |
| `rhythm_gate` | P、AF-like、房扑和重叠状态判断 |
| `global_detector` | 白化空间活动度和全局粗 P 窗 |
| `svd_projection` | 加权一维/二维空间投影 |
| `t_reconstruction` | P 区掩膜下的受约束 T 波重建 |
| `ta_model` | Ta-aware 判断和 offset 歧义处理 |
| `single_lead` | 小波、变化点、切线、面积和拟合 |
| `lead_quality` | 导联可靠性和无信息导联判断 |
| `uncertainty` | 参数扰动、噪声重采样和置信区间 |
| `fusion` | 最早/最晚可信时间簇融合 |
| `temporal_consistency` | 同形态簇内的拍间校验 |
| `diagnostics` | 日志、诊断图和拒判原因 |

---

## 15. 修改优先级

### P0：必须完成

- [ ] 增加采集链和通道同步检查
- [ ] 建立检测/精定界双分支
- [ ] 将等电位导联权重置零
- [ ] 使用白化后的多导联空间活动度
- [ ] 用“最早/最晚可信簇”替代全体加权中位数
- [ ] 增加清晰的拒判状态和原因
- [ ] TP 消失时切换状态空间基线
- [ ] 禁止固定 T 模板直接硬相减

### P1：显著提升鲁棒性

- [ ] 实现 P 区掩膜下的受约束 T 波重建
- [ ] 增加 Ta-aware offset 逻辑
- [ ] 保留原始、T 重建和 T 子空间三个分支
- [ ] 增加二维 SVD 子空间
- [ ] 增加导联组权重上限
- [ ] 将基线不确定度传递到边界置信区间
- [ ] 增加多尺度、多方法和参数扰动不确定度

### P2：进一步优化

- [ ] 增加局部变化点定界
- [ ] 实现多导联联合噪声重采样
- [ ] 使用呼吸相位辅助 T 模板选择
- [ ] 校准置信区间覆盖率
- [ ] 自动生成诊断图和失败原因报告
- [ ] 分离离线模式和在线固定延迟模式

---

## 16. 测试重点

### 基线

- TP 完整和 TP 完全消失
- 长时间无可靠基线观测
- 呼吸样漂移和非线性漂移
- P 波不得被基线模型吸收

### Ta

- 上一拍 Ta 尾叠加下一拍 P
- 当前 Ta 干扰 P offset
- 终末 P 与早期 Ta 无法区分
- 是否正确触发 `P_OFFSET_TA_AMBIGUOUS`

### T 波重建

- 幅度变化
- 轻微时间错位
- 呼吸导致的空间轴变化
- 边缘是否产生尖峰
- 是否错误削弱 P 波
- 无 P 的 T 波上是否凭空产生 P

### 多导联融合

- 单个过早或过晚伪边界
- 只有一个导联组支持
- 两个独立导联组支持
- 多个相关导联重复计票
- 有效导联数量变化

### 临床与噪声场景

- 正常窦律
- 低幅 P
- 双相和多切迹 P
- 碎裂 P
- 高心率、无 TP
- P-on-T
- 房性早搏
- 房扑和房颤
- 基线漂移
- 肌电噪声
- 电极运动
- 导联接反
- 通道固定延迟

---

## 17. 验收指标

除平均误差和标准差外，至少报告：

- 绝对误差中位数
- 95% 分位绝对误差
- 检出率
- 拒判率
- 无 P 节律上的假阳性率
- 按信噪比、心率、形态和重叠程度分层的误差
- Ta-aware 模式下的 offset 误差
- 置信区间实际覆盖率
- 严格边界与稳健边界的差值
- 有效导联数和有效导联组数
- 在线处理延迟和最坏运行时间

重点观察“接受率—误差”关系：

> 随着拒判门限变严，已接受结果的误差是否稳定下降。

这比只报告全部样本的平均误差更能体现系统的实际可靠性。

---

## 18. 参数冻结与外部验证

确定性算法同样可能对数据库过拟合。

建议：

- 仿真和一个数据库用于开发与选参
- 第二个数据库用于独立验证
- 第三个数据库或厂内锁定集用于最终测试
- 所有阈值、窗口、偏差修正和融合参数在测试前冻结
- 同时报告偏差修正前后的结果
- 统计置信区间按记录抽样，不按单拍独立抽样

仿真应覆盖：

- 多种 P 波形态
- 真实 P/T 模板组合
- 不同 Ta 强度和时限
- 不同 P/T 重叠程度
- 呼吸引起的轴和幅度变化
- 多导联相关噪声
- ADC 量化和不同采样率
- 导联增益、反接和通道延迟

---

## 19. 最终工程原则

1. **上采样用于降低栅格效应，不代表恢复原始信息。**
2. **较强低通适合检测，最终边界应由较宽带宽分支确认。**
3. **全局边界是单侧可信边界，不是导联结果的中心值。**
4. **Ta 波属于真实心房活动，不能直接当作基线去除。**
5. **TP 消失意味着基线可观测性下降。**
6. **固定 PR 节律下，未掩膜的 QRST 模板可能把目标 P 波一起消除。**
7. **T 波处理的目标是保持 P 边界稳定，不是追求残差最干净。**
8. **无信息导联不得输出边界，也不得参与融合。**
9. **基线、T 重建和 Ta 歧义必须进入不确定度。**
10. **无法辨识时明确拒判，优于输出错误但高置信的结果。**

---

## 20. ecgfeat 实施状态（2026-07-28）

本指南中的工程项已经在 `feature_extraction/ecgfeat` 完成首轮实现，包括：

- 采集同步、重复通道、异常增益/滤波和导联配置质控
- 带回滚保护的固定通道延迟补偿
- 离线双向/在线单向状态空间基线
- 宽带精度分支、35 Hz 检测分支、噪声协方差白化和 1–2 维 SVD
- P-on-T 的原始、T 重建和 T 空间子空间三分支
- amplitude、DoG/导数、切线、能量变化点和条件面积等多方法边界
- 基线扰动、方法扰动和跨分支差异生成的经验置信区间
- 至少 3 个有效导联和 2 个独立导联组的无链式 cluster 融合
- strict/robust 双边界、四种 P 状态及结构化拒判原因
- 拍间形态聚类、时间抖动和 CI 扩展
- 与既有共识模型互证后才写回兼容字段

实现入口、数据契约、验证结果和仍存在的限制见
[`p_wave_robust_engine_implementation.md`](p_wave_robust_engine_implementation.md)。
