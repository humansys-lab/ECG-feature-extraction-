<!-- i18n-nav -->
[中文](unified_clinical_rules_p3_implementation.md) | [English](unified_clinical_rules_p3_implementation.en.md) | [日本語](unified_clinical_rules_p3_implementation.ja.md)
<!-- /i18n-nav -->

> 翻訳に関する注意：この日本語版は参考用です。原文と相違がある場合は、原文を優先します。

<a id="unified-clinical-rules-p3-engineering-implementation"></a>
# 統一臨床ルール P3 エンジニアリングの実装

日付: 2026-07-12

ルールセット: `clinical_rules.v2` / `2026.07.4`

ステータス: エンジニアリングの実装と回帰検証のみ。これはそうではありません
臨床検証に基づくものであり、医療機器の性能を確立するものではありません。

<a id="implemented-unified-capabilities"></a>
## 統合機能の実装

- 信頼性の高いデータに基づいた一次 T 波異常と二次 T 波異常のスクリーニング
  連続先行 T 波反転または背の高い正の T 波パターン。 BBB/IVCD、
  心室肥大、ペーシング、前興奮は二次的なものとして保持されます。
  一次異常として報告されるのではなく、再分極が原因となります。
- ビート ID、分析可能なビート分母を備えた構造化された PAC/PVC 候補
  負担のパーセンテージ、形態学的コンテキスト、および代償性一時停止のサポート。 PACは
  確定AF中は対象外。参照専用のフラッター候補が下がる
  信頼性はありますが、統一された PAC 所見は抑制されません。
- 四肢誘導と前胸部の低電圧 QRS 電圧ルールを分離します。信頼できるリードが 1 つある
  または地域のしきい値を超えると、対応するすべてのリードのルールが反証されます。
  肯定的な結果を得るには、地域内のすべてのリードが必要です。
- ショート PR/PR セグメント、マルチリードからの心室前興奮投影
  デルタ波とQRSの証拠。肯定的なパターンは依存を抑制します
  伝導、肥大、虚血に関する記述。
- 第 2 度および完全な AV ブロック投影。第 2 級の出力には次のことが必要です
  機密パターンとドロップされた P 証拠の両方。ドロップされた P 証拠なし
  安定した P-QRS 分類は `indeterminate` であり、負ではありません。完全なブロック
  アップストリームの完全ブロック フラグと独立した AV リレーションが必要です
  証拠。

記述的な低電圧を除くすべての高リスク P3 ファミリはそのまま残ります
正常の場合は `optional_screen`。彼らの肯定的な発見は表面化する可能性がありますが、
欠席しても、以前の `normal_with_core_coverage` の意味は拡張されません。
臨床検証。

<a id="architecture-and-coverage-changes"></a>
## アーキテクチャとカバレッジの変更

リズムメカニズムは伝導、肥大、虚血、T 波の前に作動するようになりました。
解像度。これにより、事前興奮とリズムコンテキストを抑制または抑制することができます。
依存形態ステートメントを修飾します。

臨床概要には、コアおよびオプションの `capability_coverage` が含まれるようになりました。
家族。利用できない、または部分的なオプションの画面は表示されたままになります。
完了した通常の評価として黙って表示されるよりも。既存の
`domain_coverage` フィールドは、そのコア正規性の意味を保持します。

<a id="engineering-verification"></a>
## エンジニアリング検証

重点的な P3、臨床、リズム、ステートメント、および輸出契約スイート:

```text
150 passed, 1 deselected
```

選択を解除したテストは、使用できない外部 `snapshot_regression` をインポートします。
モジュール。

より広範な臨床、機能パイプライン、心房、レポート、バッチ、MedGemma、MI、および
再分極の選択:

```text
462 passed, 1 deselected, 2 environment failures
```

2 つの失敗は `app.py` のインポートであり、環境に
オプションの `gradio` 依存関係。それらは臨床ルールとは無関係です
変化します。

現在の抽出を含む実記録の煙チェックが含まれます。

- `JS00019`: IVCD コンテキストで二次的な T 波異常が表面化しました。
- `JS00027`: 1 つの構造化 PAC が浮上。参照のみのフラッター候補
  それを長く隠します。
- `JS00030`: PVC は表面化しましたが、PAC およびより高グレードの AV ブロック ロジックは表面化していませんでした。
  確認済みAFにより適用されます。
- `JS02212`: 3 度 AV ブロックの弱いラベルは、統合された完全なラベルを生成しました。
  AV ブロック パターン。
- `JS01355`: 安定した第 2 級分類のないドロップ P 証拠は、
  `indeterminate` としてエクスポートされます。 PAC 候補は部分的/低レベルで保持されます。
  矛盾した参照のみのフラッター証拠による信頼性。

<a id="remaining-validation-boundaries"></a>
## 残りの検証境界

- 局所的な心室前興奮の弱いラベルのスライスは非常に小さいです。
  `JS01944` には 2 つのデルタ正のリードがありますが、測定された PR 163 ミリ秒と PR セグメント
  75 ミリ秒なので、実装された short-PR パターンを正確に満たしていません。
  ラベル主導の閾値緩和を引き起こすよりも。
- 単一の局所的な第 2 度 AV ブロックの弱いラベルには、安定したドロップ P/PR がありません
  分類。波形判定は未定のままです。
- 低電圧の弱いラベル (`JS01156`) が、抽出された QRS と一致しません。
  複数のリード線のピークツーピーク振幅。実装では、
  測定された証拠を使用し、一致を強制するためにしきい値を調整しません。
- 平坦で二相の T 波形態には、さらに強力な構造が必要です
  現在の連続反転/tall-T を拡張する前に、プロデューサー コントラクトを終了する必要があります。
  ルール。
- データセットヘッダーは弱いラベルのままです。オプション画面のコア化を推進
  正常性を実現するには、ブラインド波形判定とより大きなポジティブスライスが必要です。
