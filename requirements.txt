# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# ECG / signal data
wfdb>=4.0

# Web UI
gradio>=6.0
fastapi>=0.115
httpx>=0.27
pydantic>=2.7
jsonschema>=4.18
python-multipart>=0.0.9
uvicorn>=0.30

# LLM inference (0.24 adds the StructuredOutputsParams path used by the
# local MedGemma Agent tool-call and final-verdict schemas)
vllm>=0.24.0

# AI libraries (optional / dev)
anthropic>=0.86.0
openai>=2.0
transformers>=4.50

# Data utilities
pandas>=2.0
pillow>=10.0
requests>=2.28
tqdm>=4.65

# PyTorch — install separately with the appropriate CUDA wheel
# torch>=2.0  # https://pytorch.org/get-started/locally/
